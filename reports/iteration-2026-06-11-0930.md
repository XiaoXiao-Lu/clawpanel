# Iteration 2026-06-11 (dev-log leak hardening)

## 目标
- 在不触碰当前大范围既有脏改的前提下，挑一个可隔离、可验证的安全/质量目标落地。
- 收敛生产构建里直接 console.log 泄漏调试信息的问题（模型列表、保存载荷、运行时内部状态）。
- 建立可复用的 dev 日志开关并补回归测试，防止后续再次引入裸 console.log。

## 地毯式审查摘要
- git status：本地领先 origin/main 28 提交，工作区存在上几轮遗留的大范围脏改（27 文件 +1844/-348，集中在命令面板/侧栏/助手/专家团/模型页样式与 i18n），以及未跟踪的 commandPalette i18n、docs/plans、上一轮报告。这些均为用户/历史自动化改动，本轮不覆盖、不提交。
- 基线：npm test 555/555 通过，npm run build 通过（约 3.3s）。
- 扫描发现 src 下 19 处裸 console.log，其中 main.js / models.js 已在脏改集合内（跳过），其余位于干净文件：
  - src/pages/agents.js：编辑 Agent 时把完整模型列表与整个保存载荷打到控制台（信息泄漏最严重）。
  - src/pages/chat.js：run 去重与看门狗触发日志。
  - src/lib/app-state.js：Gateway 意外停止日志。
  - src/lib/window-chrome.js：Tauri 检测日志。
- 项目已有约定：src/lib/ws-client.js 用 import.meta.env.DEV 守门 logWsDebug，并有 ws-client-logging 测试断言「生产端不得出现裸 console.log」。本轮沿用同一范式。

## 修改
- 新增 src/lib/logger.js：导出 devLog / devWarn，仅在 import.meta.env.DEV 为真时输出，isDev() 带 try/catch 兜底（Node 测试环境无 import.meta.env 时安全返回 false）。
- src/pages/agents.js：引入 devLog，将 6 处调试 console.log 改为 devLog（保留 console.error/console.warn 不变）。
- src/pages/chat.js：引入 devLog，4 处看门狗/去重日志改为 devLog。
- src/lib/app-state.js：1 处 Gateway 停止日志改为 devLog。
- src/lib/window-chrome.js：1 处 Tauri 检测日志改为 devLog。
- 新增 tests/dev-logger-gating.test.js：断言 devLog/devWarn 在非 DEV 时 early-return、读 import.meta.env.DEV，且上述四个生产文件都已 import devLog 且不再出现裸 console.log。

## 验证
- npm test：557/557 通过（较基线 +2，为新增用例）。
- npm run build：通过（约 3.7s）。
- 生产产物核验：dist 中 devLog 被压缩为 `function Ra(...e){ni()&&console.log(...e)}`，而 ni()（isDev）被 Vite 折叠为 `try{return!1}`，即生产环境下这些调试日志永不执行；dev 环境仍正常输出。

## 二次全局复盘
- 重新扫描 src 全量 console.log：剩余裸调用仅在脏改文件 main.js / models.js（本轮范围外，已记入下一轮建议），干净文件已全部收敛。
- 我的改动仅触及 6 个文件（2 新增 + 4 修改），与现有脏改集合完全不重叠，提交可干净隔离。
- 未发现本轮改动引入的连带回归：日志路径调整不改变控制流，console.error/console.warn 仍保留用于真实错误。
- 临时脚本写在 .tmp/（已被 .gitignore 忽略），不会误入提交。

## 风险
- 低。变更只把调试日志套上 dev 守卫，不影响功能控制流；类型/构建/测试均通过。
- 残留：main.js / models.js 仍有裸 console.log，因处于既有脏改中未一并处理。
- 工作区仍有大范围历史脏改与本地领先 28 提交，长期需要单独归档处理。

## 下一轮建议
- 待 main.js / models.js 脏改归档或合并后，将其裸 console.log 一并迁移到 devLog，并把这两文件加入 dev-logger-gating 测试清单。
- 评估把 ws-client.js 的 logWsDebug 也改为复用 src/lib/logger.js，统一日志开关来源。
- 处理或归档当前大范围脏改，降低后续自动化提交的归属混淆风险。
- 性能向：locale-shell / assistant / vendor-three 等 chunk 偏大，可单列一轮做拆分/懒加载。
