# ChatCraw 持续迭代报告

## 本轮目标
- 在 `main` 上重新建立全量测试与构建基线。
- 继续地毯式审查专家团、助手运行态、WebSocket 日志、UI/样式、测试、最近报告和构建输出。
- 修复专家团停止后恢复运行/恢复综合时重复专家贡献导致进度误判和重复综合的问题。

## 地毯式审查摘要
- 初始状态：工作区已有 `.workbuddy/automations/automation-1780935896660/memory.md` 修改，以及 `.workbuddy/memory/*`、`reports/ui-review-2026-06-10-072010.md`、`reports/ui-review-2026-06-10-072545.md`、`screenshots/models-*.png` 未跟踪文件；均视为既有/外部改动，未回滚、未提交。
- 阅读了自动化记忆、根目录、`package.json`、README/最近报告、核心入口、专家团运行器、助手专家团入口、样式与测试。
- 基线 `npm test` 通过 523/523；基线 `npm run build` 通过。
- 最新构建体积热点仍是主 CSS 约 630 KB、`assistant` 约 327 KB、`locale-shell`/`locale-engine`/`locale-ai` 和 Three.js vendor。
- 专家团新功能整体测试覆盖较好，但恢复运行路径对历史贡献只做简单 content 过滤，没有按专家/轮次去重。

## 修改说明
- `src/lib/expert-team-runner.js`
  - 新增 `dedupeResumeContributions()`，按专家 ID/名称与轮次归一恢复贡献。
  - 串联模式按 `expert + round` 去重，允许同一专家在不同轮次保留不同贡献。
  - 并行模式按专家去重，避免重复 transcript 项被再次送入主持综合或影响剩余工作判断。
  - `resumeExpertTeamSynthesis()`、`resumeExpertTeamRun()` 和 `getResumeRemainingWork()` 统一使用归一后的贡献。
- `tests/expert-team-runner.test.js`
  - 新增串联恢复去重测试：同一专家同一轮重复发言只保留第一条，空内容丢弃，不同轮次保留。
  - 新增并行恢复去重测试：同一专家重复贡献只保留第一条，缺少 expertId 时回退 expertName。

## 验证结果
- `node --check src/lib/expert-team-runner.js`：通过。
- `node --test tests/expert-team-runner.test.js tests/expert-teams-config.test.js tests/expert-teams-tool-i18n.test.js`：通过 28/28。
- `git diff --check -- src/lib/expert-team-runner.js tests/expert-team-runner.test.js`：通过。
- `npm test`：通过 525/525。
- `npm run build`：通过。

## 二次全局复盘
- 重新检查了 `git status`、本轮 diff、专家团运行器、助手恢复入口、专家团配置契约测试和构建输出。
- 确认本轮没有改变模型调用协议、事件类型、助手按钮行为、专家团页面配置表单或 UI 样式。
- 确认恢复运行的剩余成员判断与恢复综合输入使用同一份去重逻辑，避免 UI 和运行器对同一 transcript 得出不同进度。
- 仍发现助手页异常路径存在若干 `console.warn`/`console.error` 输出错误对象；这是既有风险，本轮未扩大。
- 外部 `.workbuddy`、UI review 报告和截图文件仍未纳入本轮提交。

## 剩余风险
- 本轮未做真实模型端到端恢复运行验收；当前验证依赖运行器单元测试和静态契约。
- 如果未来允许同一专家在并行模式中产生多份不同分工贡献，需要在贡献数据里增加更细的 step/run key。
- 主 CSS、`assistant` chunk、locale chunk 和 Three.js vendor 仍偏大。

## 下一轮建议
- 在可用浏览器环境中补做专家团停止、继续运行、继续综合、移动端溢出和键盘操作验收。
- 继续拆分助手/专家团低频运行态代码和 locale chunk。
- 对助手页异常日志做摘要化脱敏，避免错误对象携带供应商响应细节。
