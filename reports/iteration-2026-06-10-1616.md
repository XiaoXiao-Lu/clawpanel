# Iteration 2026-06-10 16:16

## 目标
- 加固专家团运行时成员解析，避免重复、空值、缺失或禁用成员导致重复调度和恢复状态错乱。
- 保持当前 `main` 开发，不切分支；只提交本轮可安全区分的改动。

## 地毯式审查摘要
- 已查看 `git status`、项目目录、`package.json`、最近迭代报告、专家团配置页、专家团运行器和相关测试。
- 基线验证：`npm test` 与 `npm run build` 均通过。
- 最近几轮主要集中在专家团运行卡片、Markdown 渲染和 UI 呈现；本轮选择补运行器边界可靠性。
- 工作区已有 `.workbuddy`、UI review 报告、截图，以及专家团配置页/样式/测试的外部未提交改动，本轮未触碰也不纳入提交。

## 修改
- `resolveMembers()` 现在会忽略空成员记录、无 id 专家、缺失专家、禁用专家。
- `resolveMembers()` 按顺序保留同一专家第一次有效成员槽，后续重复成员会被去重。
- 新增回归测试覆盖重复成员、空成员、缺失成员和禁用成员混合输入。

## 验证
- `node --test tests\expert-team-runner.test.js tests\expert-teams-config.test.js` 通过，27/27。
- `git diff --check -- src\lib\expert-team-runner.js tests\expert-team-runner.test.js` 通过。
- `npm test` 通过，531/531。
- `npm run build` 通过。

## 二次全局复盘
- 复查专家团运行器、配置页、样式、测试和最近报告后，未发现本轮去重改动引入的 P0/P1 连带问题。
- 发现外部已有专家团配置页键盘 tab/focus 改动及对应测试改动，方向偏可访问性增强，但不是本轮改动，未提交。
- 构建输出仍显示全局 CSS、assistant chunk、three vendor chunk 偏大，本轮未处理。

## 风险
- 本轮没有真实调用模型跑完整专家团，只覆盖运行器单元测试和全量构建/测试。
- 如果历史配置文件里已有重复成员，本轮会静默只运行第一条有效成员槽；这是更安全的行为，但可能改变少数异常配置的旧表现。

## 下一轮建议
- 单独验收并提交专家团配置页键盘可访问性改动，补齐浏览器桌面/移动端验证。
- 真实跑一次专家团完整流程，覆盖重复成员配置、失败恢复、继续综合和最终交付。
- 继续拆分/压缩 CSS 与助手页 chunk，降低首屏资源体积。
