# Iteration 2026-06-11 07:22

## 目标
- 补齐专家团删除团队失败的真实浏览器回归覆盖。
- 确保 `/__api/delete_expert_group` 返回 500 时不会误删团队，也不会改掉当前编辑器、主持人或成员选择状态。

## 修改
- `scripts/expert-teams-ui-smoke.mjs`
  - 新增 `checkTeamDeleteFailurePreservesState`。
  - 通过 Playwright route 注入一次 `/__api/delete_expert_group` 500 响应。
  - 断言错误 toast 出现、`smoke-review-panel` 仍存在、主持人仍为 `smoke-reviewer`、成员仍为 `smoke-planner,smoke-reviewer`。
  - 断言当前团队编辑器仍停留在原团队，名称、模式、主持人和已选成员数量不变。
  - 将结果写入 `teamDeleteFailureState`，并放在真实删除专家裁剪用例之前执行。
- `tests/expert-teams-config.test.js`
  - 增加团队删除失败 smoke 的静态守护 token，避免后续维护误删关键覆盖。

## 验证
- 基线 `npm test` 通过，535/535。
- 基线 `npm run build` 通过。
- `node --test tests\expert-teams-config.test.js` 通过，13/13。
- `git diff --check -- scripts\expert-teams-ui-smoke.mjs tests\expert-teams-config.test.js` 通过。
- `npm run expert-teams:ui` 通过，新增输出：
  - `teamDeleteFailureState.idValue="smoke-review-panel"`
  - `teamDeleteFailureState.nameValue="Smoke Review Panel"`
  - `teamDeleteFailureState.modeValue="review"`
  - `teamDeleteFailureState.moderatorValue="smoke-reviewer"`
  - `teamDeleteFailureState.selectedCount=2`
  - `memberIds=["smoke-planner","smoke-reviewer"]`
- 修改后全量 `npm test` 通过，535/535。
- 修改后 `npm run build` 通过。

## 二次全局复盘
- 复查 `git status`、项目根目录、核心脚本、专家团 smoke、专家团静态测试和最近 reports，未发现本轮引入的 P0/P1 连带问题。
- 本轮不改生产页面、样式、路由、API 或 i18n，降低了与工作区历史脏改冲突的风险。
- 专家团浏览器 smoke 目前覆盖模板创建、模板保存、成员/主持人同步、拖拽排序、空团队校验、非法 ID/模型校验、专家保存失败、团队保存失败、慢 Skills 刷新、专家删除失败、团队删除失败、删除专家裁剪、桌面和移动布局。

## 未处理发现
- 工作区仍有大量非本轮脏改，集中在模型页、命令面板、助手、侧栏、专家团生产页、样式、locale、`.workbuddy`、截图和 UI review 报告。
- `expert-teams:ui` 仍提示 bundled Chromium 缺失，并回退系统 Chrome。
- 错误 toast 主文案仍偏通用，服务端具体原因主要在技术详情中。
- 构建输出中主 CSS、assistant、locale-shell、locale-engine、vendor-three 仍偏大。
- 模型配置页下拉框滚动和按钮点击后即时刷新问题仍是高优先级 UI 遗留。

## 风险
- 新增 smoke 依赖前置用例创建的 `smoke-review-panel`、`smoke-planner` 和 `smoke-reviewer`；如果后续调整 smoke 顺序，需要同步维护数据准备。
- 工作区历史改动较多，提交时必须只暂存本轮 3 个文件。

## 下一轮建议
- 回到模型配置页，修复 combobox 下拉滚动、展开层位置和按钮点击后即时刷新反馈。
- 改善专家团保存/删除失败 toast 的主文案，让服务端错误更容易被用户看懂。
- 为专家团 smoke 抽出少量通用 helper，减少失败注入和状态断言的重复代码。
