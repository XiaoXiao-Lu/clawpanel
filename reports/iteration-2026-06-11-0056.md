# Iteration 2026-06-11 00:56

## 目标
- 补齐专家团模板选择器的真实浏览器回归覆盖。
- 验证模板创建路径能把模板字段正确写入团队草稿。
- 在当前大范围脏工作区中只提交本轮可明确归属的文件。

## 地毯式审查摘要
- 已检查 `git status`、最近提交、最近报告、`package.json`、专家团页面、专家团样式、专家团 smoke 脚本和配置测试。
- 当前 `main...origin/main [ahead 1]`，工作区仍有大量外部脏改，集中在命令面板、侧栏、模型页、助手、专家团产品代码、国际化、`.workbuddy`、截图和 UI review 报告。
- 基线 `npm test` 和 `npm run build` 均通过。
- 专家团仍是高价值区域：新增模板选择器已有空白创建兼容 smoke，但模板选择后的字段落表路径缺少浏览器级覆盖。

## 修改
- `scripts/expert-teams-ui-smoke.mjs`
  - 新增 `checkGroupTemplatePickerAppliesDraft`。
  - 校验模板弹层至少渲染 6 个模板。
  - 点击第一个模板后检查弹层关闭，且团队名称、说明、模式、最大轮次、最大并发正确写入草稿。
  - 在 `createPersistedTeam` 开头显式切回专家库，避免前置模板 smoke 的标签状态污染后续专家创建流程。
  - smoke 结果新增 `templateDraft` 输出，便于报告和后续排错。
- `tests/expert-teams-config.test.js`
  - 增加静态守卫 token，防止模板选择器 smoke 回归被误删。

## 验证
- 基线 `npm test` 通过，535/535。
- 基线 `npm run build` 通过。
- `node --test tests\expert-teams-config.test.js` 通过，13/13。
- `npm run expert-teams:ui` 通过：
  - desktop/mobile 均无横向溢出。
  - `templateDraft.templateCount=6`。
  - 第一个模板生成 `代码审查团`，模式为 `review`，`maxRounds=3`，`maxParallel=3`。
  - 持久化、慢 Skills 刷新保护、删除专家裁剪团队回归均通过。
- 修改后全量 `npm test` 通过，535/535。
- 修改后 `npm run build` 通过。
- `git diff --check -- scripts\expert-teams-ui-smoke.mjs tests\expert-teams-config.test.js` 通过。

## 二次全局复盘
- 复查本轮 diff、专家团 smoke 输出、构建输出、全量测试和当前 git 状态，未发现本轮引入的 P0/P1 连带问题。
- 本轮只修改自动化验证和静态测试，未改产品运行逻辑。
- 当前工作区仍存在大量外部脏改；本轮提交只应包含 smoke、测试和本报告。
- 构建输出显示主 CSS、assistant、locale-shell、locale-engine、vendor-three 仍是后续性能优化重点。

## 风险
- Playwright bundled Chromium 仍未安装，smoke 依赖系统 Chrome 兜底。
- 模板 smoke 当前覆盖第一个模板的字段落表，还未逐一覆盖所有模板。
- 工作区外部脏改较多，后续自动化仍需严格按文件归属暂存。

## 下一轮建议
- 为其余专家团模板补轻量参数表断言，覆盖 mode/maxRounds/maxParallel 组合。
- 单独整理当前外部脏改，尤其是模型页和命令面板相关改动。
- 安装 Playwright bundled Chromium，减少 smoke 对系统 Chrome 的依赖。
