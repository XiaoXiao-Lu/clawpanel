# Iteration 2026-06-10 23:57

## 目标
- 修复专家团真实浏览器 smoke 在新增团队模板选择器出现后无法继续创建团队的问题。
- 保持专家团持久化、慢 Skills 刷新保护、删除专家裁剪团队、桌面/移动无横向溢出回归继续可用。

## 地毯式审查摘要
- 已检查 `git status`、最近提交、最近迭代报告、项目目录、`package.json`、专家团页面、smoke 脚本和配置测试。
- 当前 `main` 与远端同步，但工作区已有大量外部脏改，集中在命令面板、侧栏、助手、专家团产品代码、样式、`.workbuddy`、UI review 报告和模型截图；本轮未覆盖这些改动。
- 基线 `npm test` 和 `npm run build` 均通过。
- 基线 `npm run expert-teams:ui` 失败：新增团队按钮现在可能先打开模板选择器，smoke 仍直接等待 `#group-id`，导致创建持久化团队时超时。

## 修改
- `scripts/expert-teams-ui-smoke.mjs`
  - 新增 `createBlankTeamDraft`。
  - 点击新增团队后，如果出现 `.expert-template-picker-overlay`，自动选择空白团队模板；未出现时保持旧流程。
  - 创建持久化 smoke 团队前改用该兼容入口，确保新旧专家团 UI 都能运行 smoke。
- `tests/expert-teams-config.test.js`
  - 增加静态断言，固定模板选择器兼容路径，防止 smoke 入口被误删。

## 验证
- `npm test` 通过，535/535。
- `npm run build` 通过。
- `node --test tests\expert-teams-config.test.js` 通过，13/13。
- `npm run expert-teams:ui` 通过：
  - desktop/mobile 均无横向溢出。
  - `persistence.memberIds=["smoke-planner","smoke-reviewer"]`。
  - `delayedSkillsRefresh.focusedId="expert-system-prompt"`。
  - `deletionPrune.memberIds=["smoke-planner"]` 且主持人已清空。
- `git diff --check -- scripts\expert-teams-ui-smoke.mjs tests\expert-teams-config.test.js` 通过。

## 二次全局复盘
- 复查本轮 diff、专家团新增团队流程、smoke 输出、构建输出和测试清单，未发现 P0/P1 连带问题。
- 本轮没有改产品运行逻辑，只让真实浏览器自动化兼容当前专家团模板选择器交互。
- 当前产品代码中专家团模板选择器、onboarding、命令面板国际化和助手专家团运行卡片仍处于外部未提交状态，本轮不纳入提交。
- 构建体积仍偏大：主 CSS、locale-shell、locale-engine、assistant 和 three vendor chunk 仍是后续性能优化重点。

## 风险
- Playwright bundled Chromium 仍未安装，当前通过系统 Chrome 兜底。
- 专家团 UI smoke 尚未覆盖模板选择器本身的模板创建路径，只覆盖空白模板兼容路径。
- 工作区外部脏改较多，如果后续继续自动提交，需要持续只提交本轮文件。

## 下一轮建议
- 为专家团模板选择器新增真实浏览器覆盖：选择一个模板后表单字段、模式、轮次和并发值应正确落表。
- 补齐“无成员保存错误提示”和“删除最后一位团队成员后的空团队状态”浏览器回归。
- 单独整理当前外部脏改，降低后续自动化提交时的误提交风险。
