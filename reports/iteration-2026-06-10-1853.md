# Iteration 2026-06-10 18:53

## 目标
- 为专家团页面补齐删除专家后的真实浏览器回归：删除专家库成员后，已保存团队必须自动裁剪成员和主持人引用。
- 继续保持专家团保存、重载、慢 Skills 返回、桌面/移动无横向溢出 smoke 可用。

## 地毯式审查摘要
- 已检查 `git status`、最近提交、最近报告、项目目录、`package.json`、专家团页面、配置测试和 UI smoke 脚本。
- 起始工作区仍有 `.workbuddy`、旧 UI review 报告和模型截图等外部脏文件，本轮未触碰、未暂存。
- 基线 `npm test`、`npm run build`、`npm run expert-teams:ui` 均通过。
- 专家团后端已有 `pruneExpertFromGroups`，静态测试也覆盖了持久化分离，但真实浏览器 smoke 尚未验证删除专家后团队 UI/API 是否同步裁剪。

## 修改
- `scripts/expert-teams-ui-smoke.mjs`
  - 新增 `checkDeletedExpertPrunesPersistedTeam`。
  - 在隔离 Web 服务中删除 `smoke-reviewer`。
  - 重载专家团页并切到已保存团队，验证 API 中专家已删除、团队主持人已清空、成员只剩 `smoke-planner`。
  - 同步验证 UI 中只选中 1 位成员，主持人下拉为空。
  - 将 `deletionPrune` 写入 smoke JSON 结果，方便自动化追踪。
- `tests/expert-teams-config.test.js`
  - 增加静态断言，固定删除专家裁剪团队的浏览器 smoke 场景。

## 验证
- 基线：
  - `npm test` 通过，534/534。
  - `npm run build` 通过。
  - `npm run expert-teams:ui` 通过。
- 修改后：
  - `node --test tests\expert-teams-config.test.js` 通过，13/13。
  - `npm run expert-teams:ui` 通过，包含 `deletionPrune`：`expertIds=["smoke-planner"]`、`moderatorExpertId=""`、`memberIds=["smoke-planner"]`、`selectedCount=1`、`moderatorValue=""`。
  - `npm test` 通过，535/535。
  - `npm run build` 通过。
  - `npm run expert-teams:ui` 通过。
  - `git diff --check -- scripts\expert-teams-ui-smoke.mjs tests\expert-teams-config.test.js` 通过。

## 二次全局复盘
- 复查本轮 diff、专家团删除链路、Web API 调用、UI smoke 输出和测试清单，未发现 P0/P1 或与本轮改动强相关的连带问题。
- 本轮未改产品运行逻辑，只把已有删除裁剪行为提升到真实浏览器回归覆盖。
- 现有外部脏文件仍未处理：`.workbuddy`、`reports/ui-review-*`、`screenshots/models-*`。
- 构建体积仍偏大：主 CSS、locale、assistant 和 three vendor chunk 尚未拆分。

## 风险
- 删除裁剪 smoke 目前覆盖删除主持成员的路径，尚未覆盖删除非主持成员、删除最后一位成员、以及删除后直接尝试运行专家团的提示。
- Playwright bundled Chromium 仍未安装，当前通过系统 Chrome 兜底。

## 下一轮建议
- 扩展专家团 UI smoke：无成员保存错误提示、新增草稿专家 Skills 慢返回/失败、技能标签弹窗打开时 Skills 返回。
- 为删除最后一位团队成员后的空团队状态补浏览器验证。
- 单独分析 `dist` 大 chunk，优先拆分主 CSS 与 locale 资源。
