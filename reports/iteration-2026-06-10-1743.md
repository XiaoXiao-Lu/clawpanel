# Iteration 2026-06-10 17:43

## 目标
- 修复专家团配置页被 Skills 扫描拖住导致主配置一直加载的问题。
- 扩展专家团真实浏览器 smoke，覆盖新建专家、新建专家团、刷新后 API/UI 仍可恢复。
- 保持 Web smoke 使用隔离状态目录，不污染用户真实 OpenClaw 配置。

## 地毯式审查摘要
- 已检查 `git status`、最近提交、最近迭代报告、项目结构、专家团页面、dev-api、tauri-api、现有专家团测试和 smoke 脚本。
- 起始工作区包含既有 `.workbuddy` 记忆文件、UI review 报告和模型截图，均未纳入本轮修改。
- 基线 `npm test`、`npm run build`、`npm run expert-teams:ui` 均通过。
- 复盘上一轮发现专家团 smoke 只验证页面加载、键盘 tab 和横向溢出，缺少保存/刷新闭环。

## 修改
- `src/pages/expert-teams.js`
  - 将专家、专家团主配置加载与 Skills 列表扫描解耦。
  - 页面先渲染专家团配置，随后后台加载 Skills 选项；Skills 失败只影响技能选择器，不再阻塞整个页面。
- `scripts/dev-api.js`
  - 支持启动时通过 `OPENCLAW_HOME` 指定默认 OpenClaw 状态目录，便于隔离 smoke 和测试数据。
- `scripts/expert-teams-ui-smoke.mjs`
  - 为 smoke 创建独立 `artifacts/expert-teams/state` 状态目录。
  - 预置最小 `clawpanel.json` 和 `openclaw.json`，绕开首次引擎选择和安装向导。
  - 真实创建两位专家和一个专家团，保存后刷新页面，再校验 API 返回、成员顺序、主持专家和 UI 选中状态。
  - 继续校验桌面/移动端无横向溢出，并过滤隔离环境下预期的 Gateway token WebSocket 噪音。
- `tests/expert-teams-config.test.js`
  - 增加断言，确保 `loadData` 不再直接等待 `api.skillsList()`。

## 验证
- `npm test` 通过，533/533。
- `npm run build` 通过。
- `node --test tests\expert-teams-config.test.js` 通过，11/11。
- `node --check scripts\expert-teams-ui-smoke.mjs` 通过。
- `npm run expert-teams:ui` 通过。
  - 桌面：`scrollWidth=1366`，`innerWidth=1366`。
  - 移动：`scrollWidth=390`，`innerWidth=390`。
  - 保存闭环：2 位专家，团队 `smoke-review-panel`，主持专家 `smoke-reviewer`，成员顺序 `smoke-planner -> smoke-reviewer`。
- `git diff --check` 通过。

## 二次全局复盘
- 复查专家团页面加载链路、dev-api 状态目录解析、smoke 脚本失败快照路径和测试断言，未发现本轮改动引入的 P0/P1。
- 本轮 smoke 期间发现新状态目录会先进入引擎选择和安装向导，已通过预置隔离配置解决。
- 仍存在构建体积偏大问题：主 CSS、locale engine/shell/ai、assistant 和 three vendor chunk 仍偏重，本轮未处理。

## 风险
- `OPENCLAW_HOME` 现在会影响 dev-api 的默认状态目录；正常用户环境未设置该变量，不应改变现有行为。
- smoke 过滤了隔离环境下的 Gateway token 相关 console error；真实页面错误仍会失败。
- Skills 后台加载失败时专家编辑器会显示已选择但未扫描的技能提示，需继续观察复杂技能目录下的交互体验。

## 下一轮建议
- 为专家团页面补一个 Skills 扫描失败/超时的 UI 状态 smoke，确认用户能保存其他字段。
- 扩展保存 smoke 覆盖固定模型校验、无成员保存错误和删除专家后团队成员裁剪。
- 单独分析 CSS 与 locale chunk 体积，优先拆分页面级样式和延迟加载大模块。
