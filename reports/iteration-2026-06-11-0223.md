# Iteration 2026-06-11 02:23

## 目标
- 补强专家团成员变更时主持人候选和保存结果的同步覆盖。
- 防止被移除成员继续作为主持人留在 UI 或持久化配置里。

## 修改
- `scripts/expert-teams-ui-smoke.mjs`
  - 新增 `checkModeratorClearsWhenMemberRemoved` 浏览器 smoke。
  - 创建独立的 `smoke-moderator-sync` 团队。
  - 先选择两个成员并指定 `smoke-reviewer` 为主持人。
  - 取消 `smoke-reviewer` 成员后，立即断言主持人下拉清空、候选项移除、成员选中数更新。
  - 保存后通过 API 校验 `moderatorExpertId` 不再保留失效成员，成员列表只剩 `smoke-planner`。
- `tests/expert-teams-config.test.js`
  - 增加 smoke 静态守护 token，避免主持人候选同步覆盖被误删。

## 验证
- 基线 `npm test` 通过，535/535。
- 基线 `npm run build` 通过。
- `node --test tests\expert-teams-config.test.js` 通过，13/13。
- `npm run expert-teams:ui` 通过，新增输出：
  - `moderatorMemberSync.groupId="smoke-moderator-sync"`
  - `beforeRemoval="smoke-reviewer"`
  - `moderatorValue=""`
  - `memberIds=["smoke-planner"]`
  - `reviewerOptionsAfterRemoval=0`
- 修改后全量 `npm test` 通过，535/535。
- 修改后 `npm run build` 通过。
- `git diff --check -- scripts\expert-teams-ui-smoke.mjs tests\expert-teams-config.test.js` 通过。

## 二次全局复盘
- 复查 `git status`、本轮 diff、专家团页面成员/主持人同步逻辑、配置测试和 smoke 输出，未发现本轮引入的 P0/P1 连带问题。
- 本轮只扩展专家团 smoke 和静态守护测试，不修改生产页面逻辑、样式、路由或构建配置。
- 现有专家团覆盖已经包含：模板草稿应用、全部模板参数、模板保存持久化、普通团队持久化、成员移除时主持人清空、慢 Skills 刷新保护、删除专家后团队裁剪、桌面/移动端布局 smoke。

## 未处理发现
- 工作区仍有大量非本轮脏改和未跟踪文件，集中在模型页、命令面板、助手、侧栏、国际化、`.workbuddy`、截图和 UI review 报告。
- Playwright bundled Chromium 仍未安装，`expert-teams:ui` 当前依赖系统 Chrome fallback。
- 构建输出中主 CSS、assistant、locale-shell、locale-engine、vendor-three 仍是后续性能优化重点。

## 风险
- 新增 smoke 增加少量 UI 验证耗时。
- 如果后续产品有意允许非成员主持人，需要同步调整该断言。

## 下一轮建议
- 为专家团成员拖拽排序和 order 保存补浏览器 smoke。
- 为专家团重复 ID、空成员、保存失败提示补错误态覆盖。
- 继续清理或归档既有报告与截图产物，降低长期自动化中的工作区噪音。
