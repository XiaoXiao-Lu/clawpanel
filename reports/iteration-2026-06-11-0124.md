# Iteration 2026-06-11 01:24

## 目标
- 将专家团模板选择器 smoke 从首个模板扩展到全部模板参数覆盖。
- 验证 6 个预设模板的 mode/maxRounds/maxParallel 都能正确落入草稿。
- 继续在大范围脏工作区中只提交本轮可明确归属的文件。

## 地毯式审查摘要
- 已检查 `git status`、最近提交、最近报告、`package.json`、专家团模板定义、专家团 smoke 和专家团配置测试。
- 当前 `main...origin/main [ahead 2]`，工作区仍有大量外部脏改，集中在命令面板、侧栏、模型页、助手、专家团产品代码、国际化、`.workbuddy`、截图和 UI review 报告。
- 基线 `npm test` 通过 535/535，`npm run build` 通过。
- 上一轮新增了第一个模板的真实浏览器 smoke；本轮继续补齐剩余模板参数覆盖，降低模板后续调整时的回归风险。

## 修改
- `scripts/expert-teams-ui-smoke.mjs`
  - 在 `checkGroupTemplatePickerAppliesDraft` 中加入 `expectedTemplates` 表。
  - 逐个点击 6 个模板并断言模板数量、弹层关闭、名称/说明已本地化、`mode`、`maxRounds`、`maxParallel`。
  - `templateDraft` 输出改为包含 6 个模板的参数摘要，方便后续排错。
- `tests/expert-teams-config.test.js`
  - 更新静态守卫 token，防止表驱动模板 smoke 被退回成单模板覆盖。

## 验证
- 基线 `npm test` 通过，535/535。
- 基线 `npm run build` 通过。
- `node --test tests\expert-teams-config.test.js` 通过，13/13。
- `npm run expert-teams:ui` 通过：
  - desktop/mobile 均无横向溢出。
  - `templateDraft.templateCount=6`。
  - 覆盖 `review`、`panel`、`creation`、`debate`、`research`、`sequential` 六种模板参数。
  - 持久化、慢 Skills 刷新保护、删除专家裁剪团队回归均通过。
- 修改后全量 `npm test` 通过，535/535。
- 修改后 `npm run build` 通过。
- `git diff --check -- scripts\expert-teams-ui-smoke.mjs tests\expert-teams-config.test.js` 通过。

## 二次全局复盘
- 复查本轮 diff、smoke 输出、全量测试、构建输出和当前 git 状态，未发现本轮引入的 P0/P1 连带问题。
- 本轮仍只改自动化验证和静态测试，没有修改产品运行逻辑。
- 外部脏改仍然很多，后续提交必须继续按文件归属精确暂存。
- 构建输出仍显示主 CSS、assistant、locale-shell、locale-engine、vendor-three 是后续性能优化重点。

## 风险
- Playwright bundled Chromium 仍未安装，真实浏览器 smoke 依赖系统 Chrome 兜底。
- 模板 smoke 已覆盖参数落表，但尚未覆盖模板选择后的保存、成员选择建议或模板角色提示文案。
- 当前工作区外部脏改多，本地已领先远端多个提交，后续 push 前需要重新确认远端状态。

## 下一轮建议
- 为模板选择器补保存路径覆盖：模板草稿填写 ID、选成员、保存、reload 后检查 mode 和参数。
- 单独整理当前模型页和命令面板相关外部脏改。
- 安装 Playwright bundled Chromium，减少本机环境差异对 smoke 的影响。
