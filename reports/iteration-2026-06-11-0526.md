# Iteration 2026-06-11 05:26

## 目标
- 补强专家库保存接口失败时的真实浏览器覆盖。
- 确认 `/__api/save_expert` 返回 500 时不会误落盘，并且用户正在编辑的专家草稿不丢失。

## 修改
- `scripts/expert-teams-ui-smoke.mjs`
  - 新增 `checkExpertSaveFailurePreservesDraft` 浏览器 smoke。
  - 通过 Playwright route 精确注入一次 `/__api/save_expert` 500 响应。
  - 保存失败后断言错误 toast 出现、`smoke-save-failure` 未被持久化。
  - 校验专家 ID、名称和系统提示词仍保留在表单草稿中。
  - 增加 `consumeExpectedConsoleError`，只消费本用例故意制造的一条 500 资源错误，避免全局吞掉真实 console error。
- `tests/expert-teams-config.test.js`
  - 增加保存失败草稿保留 smoke 的静态守护 token，避免后续维护误删关键覆盖。

## 验证
- 基线 `npm test` 通过，535/535。
- 基线 `npm run build` 通过。
- `node --test tests\expert-teams-config.test.js` 通过，13/13。
- `npm run expert-teams:ui` 通过，新增输出：
  - `expertSaveFailureDraft.idValue="smoke-save-failure"`
  - `expertSaveFailureDraft.nameValue="Smoke Save Failure"`
  - `expertSaveFailureDraft.promptValue="Preserve this prompt when save_expert returns a server error."`
  - 失败保存没有落盘 `smoke-save-failure`。
- 修改后全量 `npm test` 通过，535/535。
- 修改后 `npm run build` 通过。
- `git diff --check -- scripts\expert-teams-ui-smoke.mjs tests\expert-teams-config.test.js` 通过。

## 二次全局复盘
- 复查 `git status`、项目结构、测试目录、专家团 UI smoke、专家团静态测试、最近 reports、全量测试和构建输出，未发现本轮引入的 P0/P1 连带问题。
- 本轮没有修改生产页面、样式、路由或后端 API，只增强专家团真实浏览器回归覆盖。
- 专家团新功能当前 smoke 已覆盖模板、保存、主持人同步、成员排序、空团队校验、非法团队 ID、非法 fixed model、非法专家 ID、专家保存 500 草稿保留、慢 Skills 刷新、删除专家裁剪、桌面/移动端布局。

## 未处理发现
- 保存失败类 toast 主文案仍偏通用，具体错误主要在技术详情中；建议后续区分校验错误、网络错误、服务端错误的主提示。
- 工作区仍有大量非本轮脏改和未跟踪文件，集中在模型页、命令面板、助手、侧栏、国际化、`.workbuddy`、截图和 UI review 报告。
- Playwright bundled Chromium 仍未安装，`expert-teams:ui` 当前依赖系统 Chrome fallback。
- 构建输出中主 CSS、assistant、locale-shell、locale-engine、vendor-three 仍是后续性能优化重点。

## 风险
- 新增 smoke 增加少量 UI 验证耗时。
- 该用例依赖浏览器对 500 资源错误的 console 文案；已做局部消费，但不同浏览器文案若变化，可能需要同步调整。

## 下一轮建议
- 优化专家团保存失败提示，让校验错误和服务端错误在 toast 主文案中更可读。
- 为专家团删除、团队保存失败等服务端异常继续补草稿保留 smoke。
- 继续处理模型配置页下拉框滚动和按钮即时生效问题，避免用户反馈再次回归。
