# ChatCraw 持续迭代报告

## 本轮目标
- 在 `main` 上重新建立测试与构建基线。
- 地毯式复查项目结构、最近迭代报告、专家团配置页、专家团运行器、样式和测试。
- 优先完善专家团配置闭环，避免无成员团队保存后到助手运行阶段才失败。

## 地毯式审查摘要
- 初始状态：`main...origin/main [ahead 1]`，工作区已有 `.workbuddy/automations/automation-1780935896660/memory.md` 修改，以及 `.workbuddy/memory/*`、多份 `reports/ui-review-2026-06-10-*.md`、`screenshots/models-*.png` 未跟踪文件；均视为既有/外部改动，未回滚、未提交。
- 阅读了自动化记忆、`package.json`、最近迭代报告、根目录结构、核心 `src`/`tests` 结构、专家团配置页、专家团运行器和专家团相关测试。
- 基线 `npm test` 通过 529/529；基线 `npm run build` 通过。
- 专家团运行器已有运行时保护：无可用成员会抛出 `Expert team has no enabled members`；但配置页仍允许保存 0 成员团队，导致错误被推迟到助手运行阶段。

## 修改说明
- `src/pages/expert-teams.js`
  - `collectGroup()` 在保存前校验至少选择一位专家。
  - 无成员时抛出本地化错误 `expertTeams.groupMembersRequired`，让用户在配置页即时修正。
- `src/locales/modules/expertTeams.js`
  - 新增中英文文案：要求专家团至少选择一位专家。
- `tests/expert-teams-config.test.js`
  - 固定页面和 locale 必须包含该保存前校验契约，避免回归。

## 验证结果
- `node --test tests/expert-teams-config.test.js tests/expert-team-runner.test.js`：通过 26/26。
- `git diff --check -- src/pages/expert-teams.js src/locales/modules/expertTeams.js tests/expert-teams-config.test.js`：通过。
- `npm test`：通过 529/529。
- `npm run build`：通过。

## 二次全局复盘
- 复查了 `git status`、专家团保存路径、运行器无成员保护、专家团测试、最近报告和构建产物。
- 本轮只把已有运行时约束前移到配置页，不改变专家团数据结构、运行器、助手入口或持久化 API。
- 未发现与本轮改动强相关的新 P0/P1。
- 构建仍显示主 CSS 约 634 KB、`assistant` chunk 约 328 KB、`locale-*` 和 Three.js vendor 偏大。

## 剩余风险
- 本轮未做真实浏览器交互验收；改动为表单保存校验，单测和构建覆盖主要风险。
- 如果未来需要支持“草稿团队可无成员保存”，需要新增草稿状态而不是允许运行态团队为空。
- 工作区仍有既有/外部 `.workbuddy`、UI review 报告和模型截图未提交。

## 下一轮建议
- 对专家团配置页做浏览器验收，重点看保存错误提示、移动端成员选择器、拖拽排序和焦点状态。
- 继续拆分 `assistant.js` 专家团运行态渲染 helper，降低 assistant chunk 体积。
- 继续拆分主 CSS、locale chunk 和 Three.js vendor 的低频加载路径。
