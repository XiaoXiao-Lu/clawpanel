# Iteration 2026-06-11 10:02

## 目标
- 为新出的专家团功能补一条真实浏览器 smoke：模板选择器取消时不得生成草稿、污染当前编辑器或留下遮罩。
- 避开专家团生产页、runner、locale、样式等既有大块脏改，只加强可独立提交的质量门禁。

## 地毯式审查摘要
- 当前分支为 `main`，领先 `origin/main` 18 个本地提交；工作区仍存在大量既有未提交改动，集中在模型页、专家团生产页、助手页、命令面板、多语言、样式、`.workbuddy` 和计划文档。
- 基线 `npm test` 通过 543/543，`npm run build` 通过；说明当前脏工作区下主测试与构建仍稳定。
- 最近报告建议优先关注专家团新功能和模型页 combobox。模型页与专家团生产文件均已有既有脏改，本轮选择干净的 `scripts/expert-teams-ui-smoke.mjs` 来增强回归覆盖。
- 专家团 smoke 已覆盖模板应用、保存持久化、成员顺序、删除/保存失败、慢 Skills 刷新、桌面/移动端溢出；缺少模板弹窗取消路径的编辑器污染检查。

## 修改
- `scripts/expert-teams-ui-smoke.mjs`
  - 新增 `checkGroupTemplatePickerCancelKeepsDraft`，在专家团分组页点击新增后，通过遮罩取消模板选择器。
  - 新增 `groupEditorSnapshot`，对取消前后的 group editor 状态做快照比较。
  - 将 `templateCancel` 写入 smoke JSON 结果，便于后续定位和趋势比对。
- `tests/expert-teams-config.test.js`
  - 增加对新 smoke 检查函数、错误文案、快照 helper 和结果字段的静态守护，防止后续误删。

## 验证
- `node --test tests\expert-teams-config.test.js` 通过，13/13。
- `npm run expert-teams:ui` 通过；新增 `templateCancel` 结果显示取消后没有 group editor 草稿、无选中成员，桌面/移动端无横向溢出。
- `npm test` 通过，543/543。
- `npm run build` 通过。
- `git diff --check -- scripts\expert-teams-ui-smoke.mjs tests\expert-teams-config.test.js` 通过。

## 二次全局复盘
- 重新检查 `git status`、全局 diff stat、核心目录和本轮 diff 后，未发现与 smoke 脚本改动强相关的 P0/P1。
- 本轮没有触碰专家团生产页、runner、assistant、locale、样式或模型页既有脏改，提交边界清晰。
- 构建输出仍显示主 CSS 约 657 kB、`vendor-three` 约 576 kB，属于独立体积优化议题。

## 未处理问题
- 专家团模板选择器生产实现目前仍缺少更完整的键盘焦点/ESC 门禁，后续可以在生产页脏改可拆分后补齐。
- 专家团生产页、runner、assistant 集成和多语言仍有大量既有未提交改动，需要单独复核。
- Playwright bundled Chromium 缺失，UI smoke 当前依赖系统 Chrome fallback，新环境可能需要安装浏览器依赖。
- 模型页 combobox 与模型控制台既有改动仍需拆分验证。

## 下一轮建议
- 在不混入既有脏改的前提下，为专家团模板选择器补生产级键盘关闭、焦点恢复和可访问性验证。
- 拆分专家团生产页既有变更，重点验证长文本、失败态、移动端和多语言。
- 梳理模型页 combobox 既有重构，抽出可独立验证的滚动/定位/即时反馈提交。
