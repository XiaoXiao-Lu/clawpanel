# Iteration 2026-06-11 15:33

## Goals
- Re-run broad review from the latest automation context and protect existing dirty UI/localization work.
- Close the repeated expert-team UI smoke race where `npm run expert-teams:ui` can read `dist` while `npm run build` is still rewriting chunks.

## Carpet Review Notes
- Current branch is `main`, ahead of `origin/main`; existing dirty work remains in `.workbuddy`, command palette, expert-team/model UI, locales, styles, and `tests/openclaw-model-console-ui.test.js`.
- Reviewed root structure, `package.json`, recent reports, expert-team smoke script, CI/workflow tests, shared scripts, and reports around the known `dist` race.
- Baseline from this round context: `npm test` and `npm run build` passed before edits. The latest report explicitly recommended adding a guard for `expert-teams:ui` versus mutating `dist`.
- Expert-team production UI files are currently dirty, so this round stayed in the clean smoke/test layer.

## Changes
- `scripts/expert-teams-ui-smoke.mjs`
  - Added a pre-server `waitForDistReady()` guard.
  - The guard reads `dist/index.html`, verifies referenced entry assets exist, scans the `dist` file list, and waits until the build output signature is stable for a short window.
  - If `dist` is missing, incomplete, or changing for too long, the smoke now fails with a clear build-order message instead of producing later dynamic chunk 404s.
- `tests/expert-teams-config.test.js`
  - Added a static regression guard that ensures the smoke keeps the stable-dist preflight before `startServer()`.

## Verification
- `node --check scripts\expert-teams-ui-smoke.mjs` passed.
- `node --test tests\expert-teams-config.test.js` passed: 14/14.
- `npm test` passed: 552/552.
- `npm run build` passed.
- `npm run expert-teams:ui` passed; it reported `dist ready: 90 files, 9 entry assets stable for 1500ms`, then completed desktop and mobile checks without horizontal overflow.
- `git diff --check -- scripts\expert-teams-ui-smoke.mjs tests\expert-teams-config.test.js` passed.

## Second Review Findings
- Re-scanned project structure, `package.json`, recent reports, changed smoke/test files, and current git status after verification.
- No P0/P1 regression tied to the new guard was found.
- Existing dirty UI/localization/model files remain outside this round and were not staged.
- The smoke still depends on system Chrome fallback because bundled Playwright Chromium is unavailable in this environment.

## Remaining Risks
- The guard reduces accidental concurrent-build flakiness but does not coordinate processes with a lock; a build that starts after the stability window can still race the smoke.
- The stable signature scans current file paths, sizes, and mtimes; very unusual filesystem timestamp behavior could weaken the signal.

## Next Suggestions
- Add a lightweight build/smoke lock or CI-level serialization if this race appears again under heavier parallel automation.
- Continue expert-team UI polish once the current dirty production files can be isolated safely.
- Revisit CSS bundle splitting after the existing style and main-entry work is resolved.
