# Iteration 2026-06-11 03:24

## 目标
- 补强专家团空成员保存的真实浏览器错误态覆盖。
- 验证未选择成员时不会发出保存请求，也不会写入脏团队数据。

## 修改
- `scripts/expert-teams-ui-smoke.mjs`
  - 新增 `checkEmptyTeamSaveIsBlocked` 浏览器 smoke。
  - 创建 `smoke-empty-team` 草稿，只填写 ID、名称和说明，不选择成员。
  - 点击保存后断言不会调用 `/__api/save_expert_group`。
  - 断言错误 toast 能捕捉到“至少选择一位专家”的校验信息。
  - 通过 API 确认 `smoke-empty-team` 没有被持久化。
  - 校验错误后草稿仍停留在表单中，且成员选中数仍为 0。
- `tests/expert-teams-config.test.js`
  - 增加空成员保存 smoke 的静态守护 token，避免错误态覆盖被误删。

## 验证
- 基线 `npm test` 通过，535/535。
- 基线 `npm run build` 通过。
- `node --test tests\expert-teams-config.test.js` 通过，13/13。
- `npm run expert-teams:ui` 通过，新增输出：
  - `emptyTeamValidation.idValue="smoke-empty-team"`
  - `emptyTeamValidation.selectedCount=0`
  - toast 路径捕捉到空成员校验信息。
- 修改后全量 `npm test` 通过，535/535。
- 修改后 `npm run build` 通过。
- `git diff --check -- scripts\expert-teams-ui-smoke.mjs tests\expert-teams-config.test.js` 通过。

## 二次全局复盘
- 复查 `git status`、专家团保存错误处理、toast 组件、专项 smoke 输出、全量测试和构建输出，未发现本轮引入的 P0/P1 连带问题。
- 本轮只扩展专家团 smoke 和静态守护测试，不修改生产逻辑、样式、路由或构建配置。
- 专家团关键配置链路覆盖继续完善：模板、保存、成员/主持人同步、拖拽排序、空成员错误态、删除专家裁剪、慢刷新保护、桌面/移动端布局均有 smoke。

## 未处理发现
- 空成员错误的用户可见主文案是“保存失败”，具体“至少选择一位专家”在技术详情中；后续可考虑让校验型错误直接展示为主文案。
- 工作区仍有大量非本轮脏改和未跟踪文件，集中在模型页、命令面板、助手、侧栏、国际化、`.workbuddy`、截图和 UI review 报告。
- Playwright bundled Chromium 仍未安装，`expert-teams:ui` 当前依赖系统 Chrome fallback。
- 构建输出中主 CSS、assistant、locale-shell、locale-engine、vendor-three 仍是后续性能优化重点。

## 风险
- 新增 smoke 增加少量 UI 验证耗时。
- 当前 smoke 依赖 toast 技术详情里包含原始校验文本；若错误展示策略调整，需要同步更新断言。

## 下一轮建议
- 优化专家团校验错误展示，让用户可见主文案直接说明“请至少选择一位专家”。
- 为专家团重复 ID、非法 ID、保存失败网络错误补浏览器覆盖。
- 为专家团长文本、空状态和移动端触控细节补更细的 UI 回归。
