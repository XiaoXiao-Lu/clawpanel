# Iteration 2026-06-11 04:53

## 目标
- 补强专家库非法专家 ID 的真实浏览器错误态覆盖。
- 确认专家保存前会拦截非法 ID，不发保存请求、不落盘，并保留用户草稿。

## 修改
- `scripts/expert-teams-ui-smoke.mjs`
  - 新增 `checkInvalidExpertIdSaveIsBlocked` 浏览器 smoke。
  - 创建 `smoke invalid/expert` 专家草稿，填写名称、标题、说明和系统提示词。
  - 点击保存后断言不会调用 `/__api/save_expert`。
  - 断言错误 toast 能捕捉到 ID 字符集校验信息。
  - 通过 API 确认非法 ID 专家没有被持久化。
  - 校验错误后确认专家 ID 和名称仍保留在草稿中。
- `tests/expert-teams-config.test.js`
  - 增加非法专家 ID 保存 smoke 的静态守护 token，避免回归覆盖被误删。

## 验证
- 基线 `npm test` 通过，535/535。
- 基线 `npm run build` 通过。
- `node --test tests\expert-teams-config.test.js` 通过，13/13。
- `npm run expert-teams:ui` 通过，新增输出：
  - `invalidExpertIdValidation.idValue="smoke invalid/expert"`
  - `invalidExpertIdValidation.nameValue="Smoke Invalid Expert ID"`
  - 没有触发 `save_expert`，也没有落盘非法 ID 专家。
- 修改后全量 `npm test` 通过，535/535。
- 修改后 `npm run build` 通过。
- `git diff --check -- scripts\expert-teams-ui-smoke.mjs tests\expert-teams-config.test.js` 通过。

## 二次全局复盘
- 复查 `git status`、项目结构、最近报告、专家/专家团保存校验逻辑、专项 smoke 输出、全量测试和构建输出，未发现本轮引入的 P0/P1 连带问题。
- 本轮只扩展专家库错误态 smoke 和静态守护测试，不修改生产逻辑、样式、路由或构建配置。
- 专家团新功能关键配置链路继续完善：模板、保存、主持人同步、成员排序、空成员校验、非法团队 ID、非法专家 fixed model、非法专家 ID、删除专家裁剪、慢刷新保护、桌面/移动端布局均有 smoke。

## 未处理发现
- 校验错误的 toast 主文案仍是通用“保存失败”，具体错误文本在技术详情中；后续建议让表单校验类错误直接作为主文案展示。
- 工作区仍有大量非本轮脏改和未跟踪文件，集中在模型页、命令面板、助手、侧栏、国际化、`.workbuddy`、截图和 UI review 报告。
- Playwright bundled Chromium 仍未安装，`expert-teams:ui` 当前依赖系统 Chrome fallback。
- 构建输出中主 CSS、assistant、locale-shell、locale-engine、vendor-three 仍是后续性能优化重点。

## 风险
- 新增 smoke 增加少量 UI 验证耗时。
- 当前 smoke 依赖 toast 技术详情里包含原始校验文本；若错误展示策略调整，需要同步更新断言。

## 下一轮建议
- 优化专家团/专家库校验错误展示，让非法 ID、空成员、非法 fixed model 等错误直接显示为主文案。
- 为专家团和专家保存失败网络错误继续补浏览器覆盖。
- 继续处理模型配置页下拉框滚动和按钮即时生效问题，避免用户反馈再次回归。
