# ChatCraw 持续迭代报告

## 本轮目标
- 在 `main` 上重新建立全量测试与构建基线。
- 继续地毯式审查专家团、Chat、助手、样式、测试、最近报告和构建输出。
- 修复上一轮 Chat AI 消息反馈的闭环缺口：刷新后反馈不稳定、用户消息误显示反馈按钮、反馈文案硬编码和缺少测试契约。

## 地毯式审查摘要
- 初始状态：工作区已有 `.workbuddy/automations/automation-1780935896660/memory.md` 修改，以及 `.workbuddy/memory/*`、多份 `reports/ui-review-2026-06-10-*.md`、`screenshots/models-*.png` 未跟踪文件；均视为既有/外部改动，未回滚、未提交。
- 阅读了自动化记忆、根目录、`package.json`、`vite.config.js`、最近迭代/审查报告、测试目录、专家团运行器、助手专家团入口、Chat 页面、样式和构建输出。
- 基线 `npm test` 通过 527/527。
- 基线构建曾在并行执行时失败，错误为 Vite HTML emit 收到绝对 `index.html` 文件名；单独执行 `npm run build` 后通过，确认是 `dist` 并发写入/构建竞争风险。
- 复查专家团：本轮未发现 P0/P1 新问题；已有恢复贡献去重、工具域 i18n、运行卡片测试继续通过。
- 复查 Chat 反馈：发现反馈恢复函数未接入插入流程，历史消息未传稳定 ID，用户消息 meta 中也被追加了反馈按钮，且文案依赖中文 fallback。

## 修改说明
- `src/pages/chat.js`
  - 新增 `renderMessageFeedbackControls()`，统一渲染点赞/点踩按钮，使用共享 SVG 图标库和 locale 文案。
  - 历史消息渲染时向 `appendAiMessage()` 传入 `msg.id`，流式完成时用 `payload.runId || uuid()` 作为稳定 `data-msg-id` 并保存同一 ID。
  - `insertMessageNode()` 插入后调用 `_restoreFeedbackUI()`，让刷新或历史重绘后已投票状态恢复。
  - 移除用户消息 meta 中的反馈按钮，仅 AI 消息显示反馈控件。
- `src/lib/icons.js`
  - 新增 `thumbs-up`、`thumbs-down` 图标，避免 Chat 页重复内联 SVG。
- `src/locales/modules/chat.js`
  - 新增 `feedbackLike`、`feedbackDislike` 多语言文案。
- `src/style/chat.css`
  - 反馈按钮触控尺寸提升到 30px，并新增 `:focus-within`/`:focus-visible` 键盘可见状态。
- `tests/openclaw-chat-input-ui.test.js`
  - 新增 Chat AI 反馈契约测试，覆盖稳定 ID、AI-only 渲染、locale、图标库、键盘可见和样式尺寸。

## 验证结果
- `node --check src/pages/chat.js`：通过。
- `node --test tests/openclaw-chat-input-ui.test.js`：通过 8/8。
- `git diff --check -- src/pages/chat.js src/style/chat.css src/lib/icons.js src/locales/modules/chat.js tests/openclaw-chat-input-ui.test.js`：通过。
- `npm test`：通过 528/528。
- `npm run build`：通过。
- 浏览器验收：尝试使用 Codex in-app Browser 打开 `http://127.0.0.1:1420/#/chat`，Browser 安全策略拒绝访问该地址，并明确要求不要使用等价绕过方式；本轮未做浏览器截图/交互验收。

## 二次全局复盘
- 重新检查了 `git status`、本轮 diff、Chat 消息渲染路径、反馈样式、locale、图标库、专家团相关测试和最近报告。
- 确认本轮未改变 Chat 发送协议、WebSocket 状态机、专家团运行器、助手专家团入口、Gateway 配置或构建配置。
- 确认反馈旧硬编码中文 fallback 与重复内联 SVG 已从 Chat 页移除；用户消息不再渲染反馈控件。
- 仍观察到既有问题：`src/pages/chat.js` 和 `src/pages/assistant.js` 仍有多处直接展示/记录原始错误对象或 `e?.message || e`；主 CSS、`assistant`、locale chunk 和 Three.js vendor 体积仍偏大。
- 构建竞争风险仍存在：不要并行运行多个 Vite build 或 build+会写 dist 的任务。

## 剩余风险
- 未做真实浏览器交互验收，受 Browser 安全策略限制。
- Chat 反馈仍是本机 localStorage 级别，不会上报后端或跨设备同步；这是当前功能设计限制。
- 本地 IndexedDB 的 `saveMessage()` 目前不保存 attachments，历史图片本地恢复仍受既有实现限制。
- 错误提示脱敏仍未覆盖 Chat/Assistant 大文件中的所有路径。

## 下一轮建议
- 对 `src/pages/chat.js` 和 `src/pages/assistant.js` 的错误日志/提示做统一安全摘要化，避免泄露 token、路径或原始异常对象。
- 在允许访问本地服务时补做 Chat 反馈 hover/focus、移动端触控、历史恢复的浏览器验收。
- 拆分 `assistant`、locale 和主 CSS 体积热点，优先处理低频功能懒加载。
- 继续复查专家团真实运行态：停止、续跑、仅合成、异常专家恢复和移动端溢出。
