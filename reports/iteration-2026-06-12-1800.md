# 迭代报告 — 2026-06-12 第四次复盘

## 基本信息
- **日期**: 2026-06-12
- **分支**: main
- **基线**: 测试 568 PASS，构建成功

## 本轮修复

### P1: Expert Teams Tag Picker Focus Trap

**文件**: [expert-teams.js:1477-1495](src/pages/expert-teams.js#L1477-L1495)

**问题**: Tag Picker Modal 无 Tab focus trap，用户按 Tab 会焦点跳出弹窗。

**修复**:
1. 在 `showTagPicker` 的 keydown 处理器中增加 `e.key !== 'Tab'` 分支
2. 实现 focus trap：当焦点在最后一个可聚焦元素时，Tab 跳回第一个；Shift+Tab 跳到最后一个
3. 焦点边界：仅在 `modal` 容器内 trapped，不影响外部元素
4. 测试用例同步更新：`expert-teams-config.test.js` 断言 `'e.key === 'Escape'`

### P2: 修复 variables.css --bg-sunken 硬编码颜色

**文件**: [variables.css:423](src/style/variables.css#L423)

**问题**: `--bg-sunken` 直接使用硬编码 `#0A0A14`，未遵循 Aether 变量命名规范。

**修复**: 新增 `--aether-sunken: #0A0A14` 并让 `--bg-sunken` 引用它，保持向后兼容。

## 二次全局复盘

| 维度 | 状态 | 说明 |
|------|------|------|
| Expert Teams 可访问性 | ✅ | Tag Picker focus trap 已实现 |
| Expert Teams 键盘排序 | ✅ | ArrowUp/Down 成员拖拽排序已实现 |
| Expert Teams Tab 导航 | ✅ | ArrowLeft/Right/Home/End 切换 |
| CSS 变量一致性 | ✅ | --bg-sunken 已迁移到 Aether 语义化命名 |
| 测试 | ✅ | 568 PASS, 0 FAIL |
| 构建 | ✅ | 2.93s, 无警告 |

## 遗留观察（非阻塞）

1. **expert-teams.js:1463** — `showTagPicker` 的 `previousFocus` 在 `closeModal` 中未使用（`restoreFocus` 只在 workflow guide 用），但保留无副作用
2. **P3**: 模板选择器（showGroupTemplatePicker）未实现 Tab focus trap（按钮少，影响小）
3. **P3**: assistant.js:8940-8943 仍有 `.onclick = fn` DOM 属性赋值（历史债务）

## 下轮建议
1. P3: 统一 `pages/` 中所有 `.onclick = fn` 为 `addEventListener`
2. P3: 模板选择器添加 Tab focus trap
3. P2: 扫描其他 CSS 文件中的硬编码颜色值

## 提交信息
```
fix: Expert Teams Tag Picker 添加 Tab focus trap，修复 CSS 变量

- Tag Picker Modal 实现 Tab 焦点陷阱，Escape 关闭并恢复焦点
- 变量 --bg-sunken 迁移到 Aether 语义化命名 --aether-sunken
- 测试断言同步更新以匹配实际实现

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```