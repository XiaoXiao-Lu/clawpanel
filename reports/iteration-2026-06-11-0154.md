# Iteration 2026-06-11 01:54

## 目标
- 补强专家团模板创建后的保存与刷新持久化覆盖，避免模板草稿能打开但保存后丢失关键字段。

## 修改
- 在 `scripts/expert-teams-ui-smoke.mjs` 新增 `checkTemplateTeamSavePersists` 浏览器 smoke：
  - 从模板选择器创建产品评审团。
  - 设置稳定 ID、成员、主持人并保存。
  - 刷新页面后重新选择该团队。
  - 同时校验 API 返回和 UI 表单值中的模式、轮次、并行数、主持人、成员。
- 在 `tests/expert-teams-config.test.js` 增加静态守护，确保模板保存持久化 smoke 的关键路径、错误提示和输出字段不会被误删。

## 验证
- `npm test` 通过，535/535。
- `npm run build` 通过。
- `node --test tests\expert-teams-config.test.js` 通过，13/13。
- `npm run expert-teams:ui` 通过，确认 `templatePersistence` 输出包含 `smoke-template-product`、`panel`、`maxRounds=2`、`maxParallel=3`、`moderatorExpertId=smoke-planner`、`memberIds=["smoke-planner"]`。
- `git diff --check -- scripts\expert-teams-ui-smoke.mjs tests\expert-teams-config.test.js` 通过。

## 二次全局复盘
- 复查工作区后，当前仍存在大量非本轮脏改和未跟踪文件，未纳入本次提交。
- 本轮改动只扩展专家团 smoke 覆盖，不改变生产代码路径，不影响页面样式、构建配置或路由。
- 专家团模板相关覆盖目前已包括：模板草稿应用、空白创建、普通团队保存、模板团队保存后刷新持久化、技能延迟刷新保护、删除专家后团队成员裁剪、桌面和移动端 smoke。

## 未处理发现
- Playwright bundled Chromium 缺失，`expert-teams:ui` 依赖系统 Chrome fallback；建议后续在环境层补齐浏览器安装以减少机器差异。
- 工作区已有大量非本轮变更，后续继续提交时仍需逐文件确认 staged 范围。
- 专家团还可以继续补充排序、禁用态、长文本溢出、失败提示和移动端触控细节的自动化覆盖。

## 风险
- 新增 smoke 会增加少量 UI 验证耗时。
- 如果模板初始默认值未来有意调整，测试需要同步更新期望。

## 下一轮建议
- 为专家团成员拖拽排序和主持人候选变化补一条端到端 smoke。
- 将专家团保存失败、重复 ID、空成员等错误态纳入浏览器覆盖。
- 清理或归档已有报告与截图产物，降低长期自动化中的工作区噪音。
