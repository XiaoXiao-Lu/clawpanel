# ChatCraw 持续迭代报告

## 本轮目标
- 修复基线测试暴露的 Hermes 共享工具导入路径越界问题，恢复全量测试。
- 补强导入卫生测试，防止 Hermes lib 再次把 `src/lib/utils.js` 解析到仓库根目录。
- 改善专家团标签选择器的国际化完整性，避免英文等语言界面露出中文硬编码。

## 当前基线
- 时间：2026-06-09T14:30:52+08:00
- 分支：`codex/expert-team-model-routing`
- 初始工作树：已有 `.workbuddy/memory/2026-06-09.md` 和 `.workbuddy/memory/MEMORY.md` 两个既有修改；本轮视为用户/外部改动，未回滚、未提交。
- 基线 `npm test` 失败 1 项：`tests/openclaw-model-config-preservation.test.js` 触发 `src/engines/hermes/lib/model-combobox.js` 导入 `../../../../lib/utils.js`，Node 解析到不存在的仓库根 `lib/utils.js`。

## 地毯式审查摘要
- 浏览了 `package.json`、`vite.config.js`、README、最近迭代报告、测试目录、核心入口、路由、共享工具、Hermes lib/page、专家团页面、专家团样式和专家团运行器测试。
- 专家团主功能覆盖较完整：有导航/路由、前端 API、Tauri/dev API 持久化、运行器、助手入口、恢复执行、响应式样式等测试。
- 发现专家团标签选择器存在局部中文硬编码：选择计数、选择占位、全选/反选/确认按钮、Skills 状态说明等未走 i18n。
- 仍观察到既有长期问题：`src/lib/ws-client.js` 同时被动态和静态导入；主 chunk 与 `agent-office-scene` chunk 超过 500KB；`src/lib/expert-team-runner.js`、`src/lib/tauri-api.js`、`src/lib/ws-client.js` 体积偏大。

## 已修改文件
- `src/engines/hermes/lib/model-combobox.js`
- `src/pages/expert-teams.js`
- `src/locales/modules/expertTeams.js`
- `tests/main-import-hygiene.test.js`
- `tests/expert-teams-config.test.js`
- `reports/iteration-2026-06-09-1430.md`

## 修改说明
- 将 Hermes model combobox 的共享工具导入从 `../../../../lib/utils.js` 修正为 `../../../lib/utils.js`。
- 在 `tests/main-import-hygiene.test.js` 新增 Hermes lib 导入路径约束，禁止从 Hermes lib 越界导入根目录 `lib/utils.js`。
- 为专家团标签选择器新增 `expertTeams` 文案键：选择计数、占位、全选/取消全选/反选、Skills 状态说明和已选项兜底说明。
- 将专家团标签选择器按钮、placeholder、计数和状态说明改为 `t()` 渲染；关闭/搜索/取消/确认复用 `common` 文案。
- 在专家团配置测试中加入回归断言，防止标签选择器重新出现中文 UI 硬编码。

## 验证结果
- `node --test tests/main-import-hygiene.test.js tests/openclaw-model-config-preservation.test.js`：通过 6/6。
- `node --test tests/expert-teams-config.test.js tests/expert-team-runner.test.js`：通过 21/21。
- `npm test`：通过 492/492。
- `npm run build`：通过。
- 浏览器验证：启动本地 Vite 后，用系统 Chrome headless 检查 `http://127.0.0.1:1420/#/expert-teams`。
  - 桌面 1440x900：专家团页加载、无控制台错误、无横向溢出。
  - 移动 390x844：专家团页加载、无控制台错误、无横向溢出。
- Playwright 自带 Chromium 未安装，改用系统 Chrome `C:\Program Files\Google\Chrome\Application\chrome.exe` 完成检查。

## 二次全局复盘
- 复查了工作区状态、diff 统计、Hermes lib 导入路径、专家团页面硬编码点、专家团测试、核心入口、共享 lib 体积和构建输出。
- 未发现与本轮 import 修复或专家团文案替换强相关的新 P0/P1。
- 本轮启动的临时 Vite 进程已停止；端口 1420 最后仍由另一个已存在的 Vite/Node 进程占用，未停止该既有进程。

## 剩余风险
- `.workbuddy` 两个既有修改仍未提交，需继续视为外部改动。
- 专家团页面还有更大范围的中文工作流说明和工具分类文案，暂未纳入本轮，以避免扩大本地化改动面。
- 构建仍有既有 `ws-client.js` 混合导入警告和大 chunk 警告。

## 下轮建议
- 优先处理 `src/lib/tauri-api.js` 动态导入 `ws-client.js` 的混合导入问题。
- 继续拆分或懒加载大模块：`expert-team-runner.js`、`tauri-api.js`、`ws-client.js` 和 `agent-office-scene`。
- 分批推进专家团页面剩余文案 i18n，尤其是工作流说明、工具分类和状态提示。
