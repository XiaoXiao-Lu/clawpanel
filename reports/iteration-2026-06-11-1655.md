# Iteration 2026-06-11 16:55

## Goals
- Re-run broad review and baseline verification on `main` while preserving existing dirty UI/localization work.
- Close the follow-up from the previous lock iteration by documenting the locked build workflow and guarding the note in tests.

## Carpet Review Notes
- Current branch is `main`, ahead of `origin/main`; existing dirty work remains in `.workbuddy`, command palette, expert-team/model UI, locales, styles, and `tests/openclaw-model-console-ui.test.js`.
- Reviewed root structure, `package.json`, `CONTRIBUTING.md`, recent iteration reports, build chunking tests, CI workflow tests, build/smoke scripts, and Node process command lines.
- No stale `.tmp/dist.lock` was present before this round. Running Node processes were Codex kernels, OpenClaw gateway, MCP/npx servers, and expected local services rather than leftover build/smoke jobs.
- Baseline before edits: `npm test` passed 555/555 and `npm run build` passed through `scripts/build-with-dist-lock.mjs`.

## Changes
- `CONTRIBUTING.md`
  - Added `scripts/build-with-dist-lock.mjs` to the key scripts table.
  - Added a `构建产物锁` note requiring maintainers and automation to use `npm run build` instead of direct `vite build` or `npx vite build`.
  - Documented that `npm run build` and `npm run expert-teams:ui` share `.tmp/dist.lock` to prevent expert-team UI smoke from reading half-updated build chunks.
- `tests/build-chunking.test.js`
  - Added a static documentation guard so the build/smoke lock contract stays discoverable in `CONTRIBUTING.md`.

## Verification
- Baseline: `npm test` passed 555/555.
- Baseline: `npm run build` passed.
- `node --test tests\build-chunking.test.js` passed 4/4.
- `git diff --check -- CONTRIBUTING.md tests\build-chunking.test.js` passed.
- Post-change: `npm test` passed 555/555.
- Post-change: `npm run build` passed through `scripts/build-with-dist-lock.mjs`.

## Second Review Findings
- Re-scanned `package.json`, `CONTRIBUTING.md`, `tests/build-chunking.test.js`, latest reports, and current git status after verification.
- The new test assertions match the exact maintainer-facing note and do not touch runtime behavior.
- No P0/P1 issue connected to this round remains.

## Remaining Risks
- The lock still only protects commands that use project scripts; direct `vite build`, manual `dist` edits, or third-party tooling can still bypass it.
- The workspace has many unrelated dirty and untracked files, so future rounds should continue staging only the current iteration's files.
- CSS bundle size remains high and should be revisited once current UI/style dirty work can be safely isolated.

## Next Suggestions
- Add a CI or script-level check that blocks direct `vite build` usage in repository automation if more build entry points appear.
- Continue expert-team UI polish after existing production-file dirty work can be separated from automation changes.
- Revisit CSS splitting and critical-style extraction after the current broad style edits settle.
