# Iteration 2026-06-11 14:57

## Goals
- Re-run broad project review and baseline verification before changing code.
- Improve CI reliability so frontend regression tests run in GitHub Actions, not only during local automation.

## Carpet Review Notes
- Current branch is `main`, ahead of `origin/main`; existing dirty work remains in expert-team, model UI, command palette, locale, styles, `.workbuddy`, and planning docs.
- Reviewed root structure, `package.json`, recent reports, `vite.config.js`, `tests/build-chunking.test.js`, `.github/workflows/ci.yml`, and the current UI smoke guidance.
- Baseline `npm test` passed 550/550 and `npm run build` passed before this edit.
- CSS bundle splitting remains a meaningful performance target, but the relevant style and entry files are already dirty, so this round avoided mixing with that work.

## Changes
- `.github/workflows/ci.yml`
  - Added a `npm test` frontend test step after `npm ci` and before Rust checks/build.
- `tests/ci-workflow.test.js`
  - Added a small regression guard that checks CI installs dependencies, runs `npm test`, and runs it before `npm run build`.

## Verification
- `node --test tests\ci-workflow.test.js` passed: 1/1 test.
- `git diff --check -- .github\workflows\ci.yml tests\ci-workflow.test.js` passed.
- `npm test` passed: 551/551 tests.
- `npm run build` passed.
- `npm run expert-teams:ui` passed after re-run.

## Notes
- One parallel `npm run expert-teams:ui` run failed while `npm run build` was still rewriting `dist`, producing a dynamic chunk 404. Re-running the smoke after build completion passed, matching the earlier known dist race.
- The smoke still falls back to system Chrome because bundled Playwright Chromium is unavailable in this environment.

## Second Review Findings
- Re-scan found no P0/P1 issue connected to the CI change.
- The new test is intentionally text-based and only guards the current workflow contract; it does not validate the full GitHub Actions schema.
- Existing dirty UI/localization work was not touched or staged.

## Next Suggestions
- Add a CI or script-level guard that prevents `expert-teams:ui` from running against a concurrently mutating `dist`.
- Continue expert-team UI polish around Escape handling, focus restoration, and validation copy once the existing dirty files can be safely isolated.
- Revisit CSS bundle splitting after the current style and main-entry dirty work is resolved.
