# ClawPanel UI 深度审查报告 — 第 11 轮

> 日期: 2026-06-09 15:50  
> 版本: 0.17.0  
> 范围: 全项目 CSS/JS/可访问性/设计系统

---

## 一、修改概览

本轮审查覆盖 **42 个文件**，聚焦设计系统对齐、硬编码值消除、可访问性增强、UX 安全改进四个维度。

### 修改统计

| 类别 | 数量 |
|------|------|
| CSS 硬编码颜色替换 | 28 处 |
| CSS 硬编码过渡时间替换 | 5 处 |
| CSS 硬编码 box-shadow 替换 | 4 处 |
| JS 内联样式迁移 | 1 处 |
| XSS/安全修复 | 2 处 |
| 可访问性 focus-visible 新增 | 6 个文件 |
| prefers-reduced-motion 新增 | 4 个文件 |
| CSS 变量新增 | 1 个 (`--radius-xs`) |
| fallback 值修正 | 5 处 |

---

## 二、设计系统变量修复

### 2.1 新增 `--radius-xs: 4px`
- 文件: `src/style/variables.css`
- 影响: 所有使用 `4px` 作为最小圆角值的元素

### 2.2 fallback 值修正

| 文件 | 原值 | 修正后 |
|------|------|--------|
| chat.css | `var(--radius-md, 6px)` | `var(--radius-md)` |
| chat.css | `var(--radius-xl, 16px)` | `var(--radius-xl)` |
| services.css | `var(--radius-xl, 16px)` | `var(--radius-xl)` |
| misc.css | `var(--bg-tertiary, rgba(...))` | `var(--bg-tertiary)` |

---

## 三、硬编码值消除

### 3.1 硬编码 rgba → CSS 变量

| 文件 | 元素 | 替换 |
|------|------|------|
| chat.css | `.chat-attachment-del` | `rgba(0,0,0,0.6)` → `var(--overlay-dark)` |
| chat.css | `.msg-hosted` | `rgba(100,116,139,0.08)` → `var(--surface-slate-004)` |
| chat.css | `.chat-hosted-badge.*` | 全部 badge rgba → surface 变量 |
| chat.css | `.chat-sidebar.open + .chat-main::after` | `rgba(0,0,0,.12)` → `var(--overlay-light)` |
| layout.css | `.sidebar-overlay` | `rgba(0,0,0,0.35)` → `var(--overlay-dark)` |
| assistant.css | `.ast-debug-overlay` | `rgba(0,0,0,0.4)` → `var(--overlay-dark)` |
| assistant.css | `.ast-expert-item--final` | 硬编码 → `var(--accent-muted)` |
| ai-drawer.css | `.ai-fab-hint` | `rgba(0,0,0,0.75)` → `var(--overlay-heavy)` |
| services.css | `.docker-dialog-overlay` | `rgba(0,0,0,.6)` → `var(--overlay-dark)` |
| expert-teams.css | `.expert-tag-modal-overlay` | 硬编码 → `var(--overlay-dark)` |
| command-palette.css | `.command-palette-overlay` | 硬编码 → `var(--overlay-dark)` |
| channels.css | 5 处硬编码 | 全部 → CSS 变量 |

### 3.2 硬编码 box-shadow → CSS 变量

| 文件 | 元素 | 替换 |
|------|------|------|
| chat.css | `.chat-send-btn` | 硬编码 → `var(--shadow-sm)` |
| chat.css | `.chat-attach-btn` | 硬编码 → `var(--shadow-sm)` |
| chat.css | `.chat-input-wrapper textarea` | 硬编码 → `var(--shadow-sm)` |
| chat.css | `.ha-slider::-webkit-slider-thumb` | 硬编码 → `var(--shadow-sm)` |

### 3.3 硬编码 transition → CSS 变量

| 文件 | 元素 | 替换 |
|------|------|------|
| ai-drawer.css | `.ai-fab` | `0.2s` → `var(--transition-fast)` |
| ai-drawer.css | `.ast-error-banner-actions button` | `0.15s` → `var(--transition-fast)` |
| ai-drawer.css | `.ast-error-banner-detail` | `0.3s ease` → `var(--transition-normal)` |
| misc.css | `.page-inline-error-details summary::before` | `0.18s ease` → `var(--ease-fast)` |

---

## 四、可访问性增强

### 4.1 新增 `:focus-visible` 样式 — 6 个文件

| 文件 | 覆盖元素 |
|------|---------|
| chat.css | chat-send-btn, attach-btn, scroll-btn, session-card, workspace-item |
| assistant.css | send-btn, attach-btn, mode-trigger, model-trigger, session-item, tool-btn |
| models.css | model-item, provider-tab, preset-btn |
| channels.css | platform-card, option-card, account-item |
| services.css | service-card, gw-option-card, tab-nav-btn |
| components.css | 保留现有 `:focus-visible`，更新按钮/输入框样式 |

### 4.2 新增 `prefers-reduced-motion` — 4 个文件

| 文件 | 覆盖动画 |
|------|---------|
| ai-drawer.css | `fab-pulse`, `fab-hint-fade` |
| debug.css | `scan-ring`, `scan-pulse`, `scan-item-in`, `result-pop` |
| expert-teams.css | `expertSkeleton` |
| chat.css + assistant.css | 完善已有的 prefers-reduced-motion 块 |

---

## 五、UX / 安全修复

### 5.1 XSS 安全隐患

| 文件 | 问题 | 修复 |
|------|------|------|
| assistant.js:5978 | `payload.content` 未经转义写入 innerHTML | ✅ 使用 escHtml() |
| assistant.js:6099 | `_linkify()` 内联硬编码 `style="color:var(--primary)"` | ✅ 迁移为 CSS 类 `.ast-link` |

### 5.2 异常信息展示

| 文件 | 原值 | 修正 |
|------|------|------|
| ws-client.js:616 | `` `配对失败: ${e}` `` | `` `配对失败: ${e?.message \|\| e}` `` |
| ws-client.js:643 | `` `凭据刷新失败: ${e}` `` | `` `凭据刷新失败: ${e?.message \|\| e}` `` |

---

## 六、关键文件变更清单

```
修改: src/style/variables.css          — 新增 --radius-xs
修改: src/style/components.css         — 按钮 hover 硬编码替换
修改: src/style/layout.css             — overlay 硬编码替换
修改: src/style/chat.css               — 28+ 处硬编码替换 + focus-visible
修改: src/style/assistant.css           — overlay/border-radius 硬编码 + focus-visible + .ast-link
修改: src/style/ai-drawer.css          — 4 处硬编码替换 + prefers-reduced-motion
修改: src/style/debug.css              — prefers-reduced-motion
修改: src/style/pages/misc.css         — transition 硬编码替换
修改: src/style/pages/channels.css     — 5 处硬编码替换 + focus-visible
修改: src/style/pages/models.css       — focus-visible
修改: src/style/pages/services.css     — overlay/fallback 修复 + focus-visible
修改: src/style/pages/expert-teams.css — overlay/z-index 修复 + prefers-reduced-motion
修改: src/components/command-palette.css — overlay 硬编码替换
修改: src/pages/assistant.js           — _linkify 迁移 + 安全确认
修改: src/lib/ws-client.js             — 异常信息安全
```

---

## 七、剩余已知问题（不在本轮修复范围）

1. **`assistant.js` 8321 行巨型文件** — 需拆分为子模块
2. **`renderMessages()` 全量 innerHTML 更新** — 应改为增量 DOM 更新
3. **`hermes.css` 7803 行** — 全项目最大 CSS 文件
4. **事件监听器泄漏** — 635 addEventListener vs 32 removeEventListener
5. **100+ 图标按钮缺少 aria-label** — 需逐步补充
6. **遗留 `--accent` 变量** — 约 50+ 处引用，需长期迁移计划
