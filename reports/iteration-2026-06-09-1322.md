# ChatCraw 持续迭代报告

## 本轮目标
- 修复设置/插件卡片仍引用 `--accent-bg` 但全局变量缺失的问题，避免内置插件徽标背景掉样式。
- 补强设计系统测试：扫描所有 CSS 文件，要求无 fallback 的 CSS 变量必须有定义或明确属于运行时注入变量。

## 当前基线
- 时间：2026-06-09T13:22:51+08:00
- 分支：`codex/expert-team-model-routing`
- 初始工作树：已有 50+ 个 UI/CSS/组件未提交改动，以及 `.workbuddy/`、`screenshots/`、`login-auth-system/`、旧 reports、`tests/login-overlay-security.test.js` 等未跟踪内容；本轮视为既有工作，未回滚、未整体提交。
- 初始验证：`npm test` 通过 489/489；`npm run build` 通过，但仍有既有 Vite 动态/静态混合导入和大 chunk 警告。

## 地毯式审查摘要
- 浏览了 `package.json`、`vite.config.js`、README、最近报告、测试目录、核心入口 `src/main.js`、样式分层、Hermes/Xintian 引擎样式、事件监听/`innerHTML`/本地存储热点。
- CSS token 临时扫描发现 13 个未全局定义的变量名；其中 `--theme-reveal-x/y`、`--xt-i`、`--hm-warning`、`--adv-color`、`--file-color*` 等带 fallback 或运行时注入，不宜直接全局补。
- `--accent-bg` 没有 fallback，且 `src/style/pages/settings.css` 的 `.plugin-badge-builtin` 直接使用，会导致该徽标背景声明失效。
- `--accent-border`、`--accent-subtle` 仍被旧样式引用，虽然多数引用已有 fallback，但继续保留 alias 有利于旧页面过渡。

## 已修改文件
- `src/style/variables.css`
- `tests/design-system-polish.test.js`
- `reports/iteration-2026-06-09-1322.md`

## 修改说明
- 在 `src/style/variables.css` 的 legacy accent 区补回 `--accent-bg`、`--accent-border`、`--accent-subtle`。
- 在 `tests/design-system-polish.test.js` 扩展 legacy token 断言，锁定 `--accent-bg` 和 `--accent-subtle` 必须跟随 `--brand-faint`。
- 新增 CSS 变量扫描测试：遍历 `src/**/*.css`，无 fallback 且未定义的变量会失败；当前只允许 `--agent-color` 这类由 `agent-office-scene.js` inline style 注入的运行时变量。

## 验证结果
- `node --test tests/design-system-polish.test.js`：通过 5/5。
- `npm test`：通过 490/490。
- `npm run build`：通过。

## 构建与性能备注
- Vite 仍提示 `router.js`、`tauri-api.js`、`app-state.js`、`ws-client.js` 同时被动态和静态导入，导致部分动态拆包无效。
- `agent-office-scene` chunk 约 661KB，主 `index` chunk 约 1061KB，仍超过 500KB。
- 本轮未处理拆包，因为 `vite.config.js` 已在开始时存在既有脏改，直接改动会增加提交隔离风险。

## 二次全局复盘
- 复查了工作区状态、核心入口、样式导入顺序、设计 token 使用点、测试入口和构建输出。
- 未发现与本轮 alias 修复强相关的新 P0/P1。
- 复盘确认 `--accent-bg` 只有 `settings.css` 无 fallback 使用；`--accent-border` 和 `--accent-subtle` 的旧引用仍保留 fallback，但现在也能从全局 token 正常解析。

## 剩余风险
- 工作树仍有大量既有未提交 UI/CSS/组件改动和未跟踪目录；后续提交必须继续使用文件/patch 边界隔离。
- `src/style/variables.css` 仍包含大量既有设计系统改动，本轮只处理可复现的 accent alias 缺口，没有全面接受或清理整份脏改。
- 生产构建仍有混合导入和大 chunk 警告，首屏和缓存效率仍有优化空间。
- `src/main.js`、`src/lib/ws-client.js`、`src/pages/assistant.js` 等文件仍保留较多运行时 `console.log`，后续可按诊断等级治理日志噪声。

## 下轮建议
- 选一个 Vite 混合导入点做小步拆包治理，优先 `router.js` 或 `tauri-api.js`。
- 继续将 CSS token 扫描扩展到 JS inline style 模板，覆盖更多运行时样式字符串。
- 在保持现有脏改隔离的前提下，审查 `ws-client.js` 和 `assistant.js` 的生产日志泄漏风险。
