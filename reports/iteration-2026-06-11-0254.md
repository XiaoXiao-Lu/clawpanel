# Iteration 2026-06-11 02:54

## 目标
- 补强专家团成员拖拽排序的真实浏览器覆盖。
- 验证成员顺序在 UI 重排、保存 API、刷新回显三处保持一致。

## 修改
- `scripts/expert-teams-ui-smoke.mjs`
  - 新增 `checkMemberDragOrderPersists` 浏览器 smoke。
  - 创建独立的 `smoke-order-sync` 团队，选择 `smoke-planner` 和 `smoke-reviewer`。
  - 通过页面 drag/drop 事件路径把第二个成员拖到第一位。
  - 校验拖拽后 UI 顺序与隐藏 order 值更新为 `1,2`。
  - 保存并刷新后，通过 API 和重新渲染的成员列表校验顺序仍为 `smoke-reviewer,smoke-planner`。
  - 新增 `selectedMemberRows` 辅助读取选中成员、order 和序号标签。
- `tests/expert-teams-config.test.js`
  - 增加排序 smoke 的静态守护 token，避免拖拽持久化覆盖被误删。

## 验证
- 基线 `npm test` 通过，535/535。
- 基线 `npm run build` 通过。
- `node --test tests\expert-teams-config.test.js` 通过，13/13。
- `npm run expert-teams:ui` 通过，新增输出：
  - `memberOrderDrag.groupId="smoke-order-sync"`
  - 拖拽前顺序为 `smoke-planner -> smoke-reviewer`
  - 拖拽后顺序为 `smoke-reviewer -> smoke-planner`
  - API 持久化顺序为 `smoke-reviewer:1, smoke-planner:2`
- 修改后全量 `npm test` 通过，535/535。
- 修改后 `npm run build` 通过。
- `git diff --check -- scripts\expert-teams-ui-smoke.mjs tests\expert-teams-config.test.js` 通过。

## 二次全局复盘
- 复查 `git status`、本轮 diff、专家团拖拽排序实现、专项 smoke 输出、全量测试和构建输出，未发现本轮引入的 P0/P1 连带问题。
- 本轮只扩展专家团 smoke 和静态守护测试，不修改生产逻辑、样式、路由或构建配置。
- 专家团关键配置链路覆盖继续完善：模板、保存、成员/主持人同步、拖拽排序、删除专家裁剪、慢刷新保护、桌面/移动端布局均有 smoke。

## 未处理发现
- 工作区仍有大量非本轮脏改和未跟踪文件，集中在模型页、命令面板、助手、侧栏、国际化、`.workbuddy`、截图和 UI review 报告。
- Playwright bundled Chromium 仍未安装，`expert-teams:ui` 当前依赖系统 Chrome fallback。
- 构建输出中主 CSS、assistant、locale-shell、locale-engine、vendor-three 仍是后续性能优化重点。

## 风险
- 新增 smoke 增加少量 UI 验证耗时。
- 该 smoke 使用合成 drag/drop 事件覆盖页面事件链路；若未来改用不同拖拽库，测试需要同步更新触发方式。

## 下一轮建议
- 为专家团重复 ID、空成员、保存失败提示补错误态覆盖。
- 为专家团长文本、空状态和移动端触控细节补更细的 UI 回归。
- 继续清理或归档既有报告与截图产物，降低长期自动化中的工作区噪音。
