# Iteration 2026-06-11 06:30

## 目标
- 修复专家团删除接口失败时缺少用户反馈的问题。
- 覆盖专家删除 500 场景，确保失败不会误删专家、主持人或团队成员关系。

## 修改
- `src/pages/expert-teams.js`
  - 为删除专家和删除专家团的 API 调用补充 `try/catch`。
  - 失败时展示 `humanizeError(e, t('expertTeams.deleteFailed'))` 错误 toast。
- `src/locales/modules/expertTeams.js`
  - 新增 `deleteFailed` 中英文文案。
- `scripts/expert-teams-ui-smoke.mjs`
  - 新增 `checkExpertDeleteFailurePreservesState` 浏览器 smoke。
  - 通过 route 注入一次 `/__api/delete_expert` 500 响应。
  - 断言错误 toast 出现、`smoke-reviewer` 仍存在、团队主持人和成员关系未被裁剪、当前编辑器仍选中原专家。
- `tests/expert-teams-config.test.js`
  - 增加删除失败处理和 smoke 的静态守护 token。

## 验证
- 基线 `npm test` 通过，535/535。
- 基线 `npm run build` 通过。
- `node --test tests\expert-teams-config.test.js` 通过，13/13。
- 修改后 `npm run build` 通过。
- 修改后 `npm run expert-teams:ui` 通过，新增输出包含：
  - `expertDeleteFailureState.idValue="smoke-reviewer"`
  - `expertDeleteFailureState.nameValue="Smoke Reviewer"`
  - `expertDeleteFailureState.moderatorExpertId="smoke-reviewer"`
  - `expertDeleteFailureState.memberIds` 保留 `smoke-reviewer`
  - toast 主体包含 `删除失败` 和注入的服务端错误。
- 修改后全量 `npm test` 通过，535/535。
- 最终 `npm run build` 通过。
- `git diff --check` 通过。

## 二次全局复盘
- 复查 `git status`、核心入口、测试、专家团页面、i18n 模块、UI smoke 和最近 reports，未发现本轮引入的 P0/P1 连带问题。
- 专家团保存失败、团队保存失败、慢 Skills 刷新、删除专家裁剪、桌面/移动布局已有 smoke 覆盖；本轮补上了专家删除失败状态保留。
- 工作区仍存在大量历史脏改，提交时必须只暂存本轮删除失败相关 hunks 和测试/report 文件。

## 未处理发现
- 删除专家团失败虽然生产逻辑已补 `catch`，但还没有单独的浏览器 500 smoke。
- 保存/删除失败 toast 仍可进一步区分校验、网络和服务端错误，主文案可以更精确。
- Playwright bundled Chromium 仍未安装，`expert-teams:ui` 依赖系统 Chrome fallback。
- 构建输出中主 CSS、assistant、locale、three 相关 bundle 仍偏大，后续可拆分。
- 模型配置页仍有下拉滚动和按钮即时刷新反馈历史问题，建议继续回归处理。

## 风险
- 新增 smoke 依赖前置用例创建的 `smoke-reviewer` 和 `smoke-review-panel` 数据；如果后续调整执行顺序，需要同步维护数据准备。
- 当前工作区历史改动较多，本轮提交需要精确暂存，避免混入其它迭代内容。

## 下一轮建议
- 为专家团删除失败补单独 UI smoke，并验证 selectedGroupId 和团队列表不变。
- 改善专家团错误 toast 的可读性，让服务端错误与通用失败原因更清晰。
- 继续处理模型配置页 combobox 展开滚动和按钮点击后即时刷新问题。
