# 2026-06-07 工作日志

## 修复模型设置页面的三个 Bug

1. **添加删除服务商按钮** — 服务商卡片头部缺少 `data-action="delete-provider"` 按钮，后端逻辑已有但 UI 遗漏。在 `renderProviderCard` 的 actions 区添加了红色垃圾桶按钮。
2. **修复批量删除/测试的 CSS 选择器** — `handleBatchDelete` 和 `handleBatchTest` 使用了不存在的 `.model-checkbox:checked` 选择器，改为正确的 `.model-item__cb:checked`。
3. **修复编辑模型 ID 后引用不更新** — `editModel` 修改模型 ID 后 primary/fallbacks 中的旧引用不会更新。新增 `replaceModelReferences` 函数，在 ID 变化时自动替换引用。

文件：`src/pages/models.js`

## 修复 Agent 备份和主题切换功能缺陷

1. **Agent 卡片添加备份按钮** — `agents.js` 中 `backupAgent()` 函数已实现但 UI 无入口，在卡片操作区添加了备份按钮。
2. **Agent 详情页添加备份按钮** — `agent-detail.js` 页面头部添加了备份按钮和对应的 `backupAgent` 处理函数。
3. **命令面板主题切换空实现** — `main.js` 中命令面板的主题切换命令是空的，已连接到 `toggleTheme()` 函数。

文件：`src/pages/agents.js`, `src/pages/agent-detail.js`, `src/main.js`

## 统一 z-index 层级系统

1. **添加 CSS 变量** — 在 `variables.css` 中定义 `--z-base` 到 `--z-max` 的层级系统
2. **替换所有硬编码 z-index** — 统一替换 layout.css、chat.css、assistant.css、components.css、ai-drawer.css、command-palette.css 中的混乱值
3. **修复 main.js 中的内联样式** — 将 banner 的 z-index 改为使用变量

文件：`src/style/variables.css`, `src/style/*.css`, `src/main.js`

## 提取硬编码颜色为 CSS 变量

1. **语法高亮颜色** — 提取 `--hl-keyword`, `--hl-string` 等 6 个变量
2. **导航图标颜色** — 提取 `--nav-assistant`, `--nav-chat` 等 15 个变量
3. **替换 chat.css 中的 badge 颜色** — 使用 `--success`, `--warning`, `--error` 等语义变量

文件：`src/style/variables.css`, `src/style/chat.css`, `src/style/layout.css`

## 国际化改进

1. **窗口控制按钮** — `window-chrome.js` 中的 Minimize/Maximize/Close 使用 `t()` 翻译
2. **Agent 办公室场景** — `agent-office-scene.js` 中的状态标签和属性名使用 `t()` 翻译

文件：`src/lib/window-chrome.js`, `src/components/agent-office-scene.js`

## Docker 管理兼容性

1. **Tauri 模式提示** — 在 services.js 中，Tauri 模式下显示 Docker 管理不可用的提示，而不是完全隐藏

文件：`src/pages/services.js`

## UI 交互改进

1. **按钮加载状态** — 为 chat-debug、dashboard、models、diagnose 页面的异步操作按钮添加 `btn-loading` 类和旋转动画
2. **统一加载状态辅助函数** — 添加 `setBtnLoading` 工具函数管理按钮加载状态
3. **清理内联样式** — 提取 about.js 中 Discord 按钮的硬编码样式为 CSS 类
4. **空状态优化** — 确保 empty-state 组件在各页面一致使用

文件：`src/pages/chat-debug.js`, `src/pages/dashboard.js`, `src/pages/models.js`, `src/pages/diagnose.js`, `src/pages/about.js`

## 清理内联样式（setup.js / dreaming.js / communication.js / chat-debug.js）

1. **setup.js** — 提取 50+ 处内联 style 属性为 CSS 类：
   - 添加 `.setup-step-section`, `.setup-step-title`, `.setup-step-desc`, `.setup-step-box`, `.setup-step-actions` 等 30+ 个类
   - 提取表单输入样式为 `.setup-input-compact`, `.setup-textarea-compact`, `.setup-select`
   - 提取详情/摘要样式为 `.setup-details`, `.setup-details-summary`, `.setup-details-body`
   - 提取安装面板样式为 `.setup-panel-title`, `.setup-panel-desc`, `.setup-source-options`, `.setup-source-option`
   - 提取状态提示样式为 `.setup-result`, `.setup-warning-note`, `.setup-inline-code`

2. **dreaming.js** — 提取所有内联 style 为 `.dream-*` CSS 类：
   - Hero 区域：`.dream-hero-body`, `.dream-hero-main`, `.dream-hero-badge`, `.dream-hero-title`, `.dream-hero-desc`, `.dream-hero-text`, `.dream-hero-tags`, `.dream-hero-actions`
   - 统计卡片：`.dream-stat-cards`, `.dream-phase-grid`, `.dream-lane-grid`
   - Phase 卡片：`.dream-phase-card`, `.dream-phase-header`, `.dream-phase-details`
   - 条目列表：`.dream-entry`, `.dream-entry-snippet`, `.dream-entry-path`, `.dream-entry-meta`
   - Dream Lane：`.dream-lane`, `.dream-lane-item`, `.dream-lane-dot`, `.dream-lane-content` 等
   - 日记视图：`.dream-diary-header`, `.dream-diary-grid`, `.dream-diary-item`, `.dream-diary-pre` 等
   - 状态提示：`.dream-hint-blocked`, `.dream-hint-unsupported`, `.dream-hint-error`
   - 加载/错误状态：`.dream-skeleton-hero`, `.dream-skeleton-body`, `.dream-connecting`, `.dream-error-card`

3. **communication.js** — 提取内联样式为 `.comm-*` 类：
   - `.comm-toolbar`, `.comm-toolbar-spacer`, `.comm-skeleton`, `.comm-error`
   - `.comm-approval-item`, `.comm-approval-item-main`, `.comm-approval-item-cmd`, `.comm-approval-item-meta`, `.comm-approval-item-badge`
   - `.comm-approvals-desc`, `.comm-approvals-queue`, `.comm-approval-queue`, `.comm-approvals-unsupported`
   - 表单工具类：`.form-group--row`, `.form-label--compact`, `.form-hint--compact`, `.form-input--short`, `.form-input--medium`

4. **chat-debug.js** — 提取内联样式：
   - 将 `style="animation:scan-ring 1s linear infinite"` 改为 `.scan-icon--spin` 类
   - 将 `style="animation-delay:${i * 60}ms"` 改为 `[data-index]` 属性选择器
   - 将高级工具按钮的 flex 布局改为 `.adv-btn-grid`
   - 将 `advLog` 中的内联颜色改为 CSS 变量 `--adv-color`

5. **CSS 文件更新**：
   - `src/style/pages/channels.css` — 添加所有 setup 相关样式（约 50 条规则）
   - `src/style/components.css` — 添加 communication 和 form 工具类
   - `src/style/debug.css` — 添加 `.adv-btn-grid`, `.scan-icon--spin`, `[data-index]` 动画延迟规则

文件：`src/pages/setup.js`, `src/pages/dreaming.js`, `src/pages/communication.js`, `src/pages/chat-debug.js`, `src/style/pages/channels.css`, `src/style/components.css`, `src/style/debug.css`

## 清理仪表盘和推送通知内联样式

1. **dashboard.js** — 提取 20+ 处内联样式为 CSS 类：
   - 日志查看器：`.dashboard-log-viewer`（max-height）
   - 错误降级卡片：`.dashboard-error-card`
   - 警告提示（foreignGateway/multiInstall）：`.dashboard-warning-hint`, `.dashboard-warning-actions`
   - 小号元数据：`.dashboard-meta-xs`, `.dashboard-meta-ok`, `.dashboard-action-row`
   - 概览卡片图标颜色：`.overview-card-icon--accent`, `--warning`, `--success`, `--muted`
   - 概览小号值：`.overview-value-sm`
   - WebSocket 状态：`.ws-dot`, `.ws-detail`
   - 渠道标签：`.channel-chip`, `.channel-chip-dot`, `.channel-section`, `.channel-section-title`
   - 会话状态：`.session-section`
   - 日志行：`.log-line-flex`, `.log-line-time`, `.log-line-msg`, `.log-empty`
   - 日志等级徽章：删除 `LOG_LEVEL_STYLE` 对象，改用 CSS 类 `log-level-badge--*`
   - 删除 JS 中 `card.style.cursor = 'pointer'`，改为 CSS `.overview-card[data-nav]`
   - 删除 `const LOG_LEVEL_STYLE = { ... }` 常量对象

2. **notifications.js** — 提取 4 处内联样式：
   - 加载状态：`.push-loading`
   - 端点展示：`.push-endpoint-hint`, `.push-endpoint-code`
   - 操作按钮：`.push-actions`
   - 提示文本：`.push-hint`

文件：`src/pages/dashboard.js`, `src/pages/notifications.js`, `src/style/pages/dashboard.css`

## 聊天页面渲染性能重构 (Phase 5)

重构 Hermes 聊天页面的渲染架构，解决实时聊天同步卡顿问题：

1. **mdToHtml 缓存** — 添加 `_mdCache` Map（最大 300 条），以 `消息ID:内容hash:长度` 为 key 缓存 Markdown 解析结果，避免每条消息每次重绘都重新解析。`renderMessage()` 改用 `cachedMdToHtml()`。

2. **流式增量 DOM 更新** — 替换 `store.subscribe(() => draw())` 为智能订阅：
   - 流式期间检测到同一消息内容/tools 变化 → 仅更新该消息的 `.hm-chat-msg-content` div 和 live tools，不重建全页
   - 其他情况（会话切换、流式启停、新消息等）走完整 draw()
   - 新增 `incrementalStreamingUpdate()` 函数处理增量更新

3. **CSS 虚拟滚动** — `.hm-chat-msg` 添加 `content-visibility: auto; contain-intrinsic-size: auto 80px`，浏览器自动跳过离屏消息的布局和绘制

文件：`src/engines/hermes/pages/chat.js`, `src/engines/hermes/style/hermes.css`

## 模型配置页添加并发上限编辑

在 OpenClaw 模型配置页的「模型路由控制台」底部添加并发上限输入框：

1. **渲染** — `renderConsole()` 中读取 `state.config.agents.defaults.maxConcurrent`，显示数字输入框（min=1, max=100）
2. **事件绑定** — `onchange` 时校验范围、写入 config、触发 autoSave
3. **默认值** — `ensureConfigDefaultModelConfig()` 中若未设置则默认 4
4. **国际化** — `models.js` 添加 `maxConcurrent` / `maxConcurrentHint` 翻译键
5. **样式** — `models.css` 添加 `.models-concurrency-inline` / `.models-concurrency-input` 样式

文件：`src/pages/models.js`, `src/locales/modules/models.js`, `src/style/pages/models.css`

## Agent 技能管理搜索功能修复

1. **Agent 详情页技能 Tab 添加搜索框** — `agent-detail.js`：在技能选择列表上方添加搜索输入框（`#agent-skill-filter-input`），实时过滤技能卡片，匹配 name/description，无结果时显示空提示
2. **已安装技能列表搜索增强** — `skills.js`：卡片添加 `data-source` 属性，搜索时匹配 name/desc/source 三个字段，搜不到时显示空结果提示
3. **商店搜索本地过滤** — `skills.js`：当全量索引 `_storeIndex` 已加载时搜索走本地过滤（零网络延迟），匹配 name/displayName/slug/description/category；切换 Agent 时不再清空 `_storeIndex` 缓存

文件：`src/pages/agent-detail.js`, `src/pages/skills.js`

## 全量 Bug 修复（P0+P1+P2）

### P0 — 潜在崩溃修复
1. **agent-detail.js** — 7 处 `querySelector().addEventListener()` 加 `?.` 空安全（行 59/212/219/226/357/419/731）
2. **skills.js** — Tab 切换（行 1158-1159）判空保护
3. **skills.js** — 移除重复事件绑定（mouseover/mousemove/pointerover → 仅 pointerover；mouseout/pointerout → 仅 pointerout）

### P1 — 功能修复
4. **agent-detail.js + skills.js** — 搜索空提示硬编码中文改为 `t('skills.noResults')` i18n
5. **agent-detail.js** — 备份路径分割兼容 Windows（`\` 和 `/` 自动识别）
6. **agents.js** — 办公室监控图例、视角按钮、状态标签、进度文本等 20+ 处硬编码中文走 i18n（新增 `agents.office*`、`agents.state*`、`agents.progress*` 等翻译 key）
7. **channels.js** — 运行态面板按钮、toast、错误提示等 10+ 处硬编码中文走 i18n（新增 `channels.runtime*` 翻译 key）

### P2 — 代码异味修复
8. **skills.js** — 添加 `cleanup()` 导出函数，清理定时器/模块级变量

文件：`src/pages/agent-detail.js`, `src/pages/skills.js`, `src/pages/agents.js`, `src/pages/channels.js`, `src/locales/modules/agents.js`, `src/locales/modules/channels.js`

### Rust 不安全 unwrap 修复
9. **config.rs:3432** — `tarball.unwrap()` → `tarball.ok_or("tarball 数据缺失")?`
10. **device.rs:62** — `serde_json::to_string_pretty(&json).unwrap()` → `.map_err(|e| format!(...))?`
11. **device.rs:104** — `duration_since(UNIX_EPOCH).unwrap()` → `.unwrap_or_default()`
12. **pairing.rs:64** — `duration_since(UNIX_EPOCH).unwrap()` → `.unwrap_or_default()`
全部通过 `cargo build` 编译验证

文件：`src-tauri/src/commands/config.rs`, `src-tauri/src/commands/device.rs`, `src-tauri/src/commands/pairing.rs`

## OpenClaw 聊天页面模型切换修复

### Bug #2：切换会话后模型选择器不更新
**`chat.js:1637-1640`** — 在 `switchSession()` 末尾添加：
```js
if (wsClient.gatewayReady && _selectedModel) {
  applySelectedModel().catch(() => {})
}
```
切换会话后自动重新应用当前选中的模型，确保新会话使用正确的模型。

### Bug #3：Gateway 就绪前模型切换不可用
**`chat.js:1345-1350`** — 在 `onReady` 回调中（Gateway 就绪后）添加：
```js
loadModelOptions().then(() => {
  if (_selectedModel) {
    applySelectedModel().catch(() => {})
  }
})
```
Gateway 连接成功后自动刷新模型列表并应用选中的模型，不再需要用户手动触发。

文件：`src/pages/chat.js`
