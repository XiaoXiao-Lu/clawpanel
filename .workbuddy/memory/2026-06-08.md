## 2026-06-08

### 模型配置页面(models.js) Bug 修复
- **问题**：从旧"服务商卡片+折叠列表"UI 重构为"Tab+统一表格视图"后，部分功能不完整
- **修复内容**：
  1. `renderProviders()`: 恢复服务商级别操作按钮（编辑/删除服务商、添加模型、获取远程模型列表），在选中具体服务商 Tab 时显示
  2. `renderProviders()`: 修复 Tab 计数问题，"全部"Tab 显示未过滤的模型总数
  3. `bindProviderButtons()`: 新增对 Tab 栏服务商级别按钮的事件绑定
  4. `locateModel()`: 修复旧 DOM 引用（`.provider-body`, `[data-action="toggle-provider"]`），改为切换到对应服务商 Tab
  5. 删除死代码 `renderModelRows()` 函数（不再被调用）
  6. `handleSelectAll/handleBatchDelete/handleBatchTest`: 修复 DOM 选择器，从旧的 `.model-item__cb` 改为新的 `.models-table-row` 结构
  7. `handleBatchTest`: 修复标记当前测试行的选择器
  8. i18n: 补充缺失的 `choosePrimary`、`fallbackCount` key，统一 `untested`→`notTested`
  9. CSS: 添加 `models-provider-tab-actions` 样式，添加 `.model-row-highlight` 动画 class
  10. icons.js: 补充缺失的 `star`、`activity`、`cpu`、`map-pin`、`layers` 图标
  11. `page.__modelsState`: 暴露 state 给 locateModel 等函数使用
  12. `renderProviders()`: 添加 checkbox 列和全选功能（表头 `#models-select-all` + 行内 `.models-row-cb`）
  13. `renderProviders()`: Tab 栏服务商操作按钮中添加"批量测试"和"批量删除"按钮，并用 divider 分隔模型操作和服务商操作
  14. `bindProviderButtons()`: 服务商级别按钮 section 参数改为传 listEl，全选 checkbox 绑定 onchange 事件
  15. `handleBatchTest()`: 修复 `renderProviders` 后重新查找按钮的旧 DOM 引用（`[data-provider]` → `#providers-list`）
  16. CSS: 添加 `.col-cb` 列宽度、`.models-row-cb` 样式、`.models-provider-tab-actions__divider` 分隔线
  17. 修复远程获取列表失败弹窗中，点击“手动添加模型”时传递的 `state` 参数错误的 Bug（改正 `{ config: state.config, save: state.save }` -> `state`）

### 专家团集成到晴辰助手
- 助手顶栏新增专家团选择器
- sendViaExpertTeam 流程 + 执行过程渲染
- expert-team-runner 支持 externalSlot

### 专家编辑器工具/Skill 弹窗模式
- textarea → modal: 搜索/全选/反选/checkbox网格

### 专家团串联(sequential)模式
- runExpertTeamSequential: 按order串行, maxRounds多轮
- assistant.js 自动按 mode 路由

### 创建专家和专家团数据
- 6位专家 + 3个团写入 ~/.openclaw/clawpanel/

### UI重设计 — Precision 暗色主题
- variables.css 完全重写: Space Black, indigo渐变, noise grain
- 卡片入场动画, 进度条, 专家结论内发光

### 侧边栏重构
- 分区可折叠 (click toggle), 顶部搜索框 (实时过滤)

### 专家团执行卡片完善
- 进度条 "3/5步", 状态标签, 过程记录折叠, 骨架屏, 错误恢复提示

### UI 重构 — Precision Noir 设计语言
- Design: warm ink backgrounds (#0A0A10 → #161622), editorial typography, generous negative space
- Typography: Space Grotesk (headings/UI) + DM Mono (code/data) via Google Fonts
- variables.css: full token refresh — warmer backgrounds, refined spacing, softer radii, Space Grotesk as font-display/sans
- index.html: Google Fonts preconnect/link, splash screen colors updated
- components.css: buttons (font-display, translateY hover), cards (gentler borders, padding), inputs (sunken bg, brand glow focus), tabs (cleaner), badges (font-display), modal (scale+fade animation)
- layout.css: sidebar (radial brand gradient instead of linear), nav items (no inset shadow, font-display, 34px), page transitions (slower 300ms ease-out), content area (radial ambient gradient)
- dashboard.css: health card (2px accent border, rounded-xl), score ring (smaller 104px, 8px stroke), next action button (font-display, lift hover), overview cards (lift hover, refined typography)
- polish.css: card hover translateY(-1px), staggered reveal animation for page children (350ms, sequential 50ms delays), reduced-motion respect
- Build verified: `vite build` succeeded cleanly

### 界面布局优化
- 侧边栏重组: "其它" 分区改名 "系统"，i18n 增加 sectionSystem (12 种语言)
- 新增上下文栏 (context-bar): 横幅下方固定显示当前页面路径（分区 / 页面名），滚动不丢失位置感
- router.js: 新增 updateContextBar() 自动在路由切换时更新上下文栏，_routeContext 映射所有路由到分区+标签
- Dashboard: 概览网格从 4+2+2+2+2+2 改为 3 列均匀网格，视觉节奏更一致
- Dashboard: 增加 .stats-heading 和 .dashboard-section-divider 样式为后续区域标题做准备
- 上下文栏移动端: 隐藏分区路径只显示页面名，节省空间
- Build 验证通过

### Dashboard 区域标题与信息层级优化
- dashboard.js: 在统计卡片、概览网格、快捷操作、日志区域前添加区域标题（核心指标 / 运行概览 / 快捷操作 / 最近日志）和分隔线
- dashboard.css: 添加 .stats-heading（大写标签样式）、.dashboard-section-divider（细线分隔），日志区域增加背景+边框+圆角
- i18n: zh-CN/en 增加 statsTitle / overviewTitle / quickActionsTitle
- 构建验证通过

### 横幅平滑过渡动画
- layout.css: update-banner 和 gw-banner 的过渡时间从 200-300ms 统一提升到 350ms ease-out，隐藏时增加 translateY(-4px) 微动效

### 页面结构统一
- layout.css: .page-header 统一增加 padding-bottom + border-bottom，所有页面标题区视觉一致
- about.js/security.js: 移除内联 style，改用 CSS class；修复缺失的 page-title class
- agents.css / dashboard.css: 同步 page-header 样式

### 模型配置页面 UI 优化
- 替换 provider card 和 fallback waterfall 的文本箭头 `▸/▾` 为 `icon('chevron-down', 12)` SVG + `rotate(90deg)` 展开态过渡动画
- 新增 `.chevron` / `.chevron--open` / `.models-empty-state` CSS class
- provider-card__body 折叠改用 `[hidden]` 属性 + `max-height` 过渡动画
- 空 provider 状态改为 `.models-empty-state` class（虚线边框圆角容器）
- 构建验证通过

### 审查后修复 (基于 code-review 报告)
- **清理死代码**: 删除 models.css 中 ~130 行无引用的 `.model-row*` 样式块
- **qtcool 横幅**: 内联 style → `.models-qtcool-body` / `--hidden` CSS class，折叠改用 classList.toggle
- **按钮图标**: provider card 操作按钮 `↻` `+` `···` 文本符号替换为 `icon('refresh-cw', 12)` / `icon('plus', 12)` / `icon('edit', 12)` SVG
- **瀑布折叠**: fallback-waterfall-container 从 `style.display='none'/'block'` 改为 `.models-waterfall-hidden/visible` CSS class 驱动
- 构建验证通过

### Dashboard 区域标题与信息层级优化
- dashboard.js: 在统计卡片、概览网格、快捷操作、日志区域前添加区域标题（核心指标 / 运行概览 / 快捷操作 / 最近日志）和分隔线
- dashboard.css: 添加 .stats-heading（大写标签样式）、.dashboard-section-divider（细线分隔），日志区域增加背景+边框+圆角
- i18n: zh-CN/en 增加 statsTitle / overviewTitle / quickActionsTitle
- 构建验证通过

### 横幅平滑过渡动画
- layout.css: update-banner 和 gw-banner 的过渡时间从 200-300ms 统一提升到 350ms ease-out，隐藏时增加 translateY(-4px) 微动效
- 横幅出现/消失不再生硬

### 页面结构统一
- layout.css: .page-header 统一增加 padding-bottom + border-bottom（1px solid var(--border-2)），所有页面标题区视觉一致
- about.js: 移除内联 style，改用 CSS class
- security.js: 补充 page-title class
- agents.css / dashboard.css: 同步 page-header 样式
- glossary.css: 尝试更新（文件被占用，稍后处理）
- 构建验证通过

### 模型配置页按钮密度优化
- **Provider card header**: 4 按钮 → 只显示 + (添加模型)，其余 3 个放进 `···` 下拉菜单（hover触发），新增 `.provider-actions-toggle` / `.provider-actions-menu` CSS
- **模型行按钮**: 4 个文字按钮 → 图标按钮 `.btn-icon`（star/activity/edit/trash），宽度减少 ~60%
- **新增 CSS**: `.btn-icon` / `.btn-icon--danger` / `.provider-actions-menu` 下拉面板样式
- 构建通过

### 模型配置页面 — 前端代码审查
- 完成对 models.js (2373行) + models.css (1611行) + model-combobox.js + model-presets.js 的全面审查
- 报告保存: reports/code-review-2026-06-08-2133.md
- 关键发现: 上帝组件(2373行)需拆分、CSS重复定义(.provider-card/.btn-ghost各两套)、全量innerHTML重建导致焦点丢失、拖拽逻辑重复、推理标签硬编码中文、控制台信息密度过高、瀑布流与控制台备选区功能重叠
- 风险等级: 中，总体评价: 待修改后合并

### 模型配置页面 — 审查后优化实施
- ✅ 推理标签硬编码 `推理` → `t('models.reasoning')`，走 i18n
- ✅ state 初始化补全: `sortBy`/`_collapsed`/`_fallback_candidates_collapsed` 默认值 + JSDoc
- ✅ 删除 CSS 重复定义: `.provider-card` 第二套(1397-1483)合并差异后删除；`.btn-ghost` 第二套(1556-1563)删除；`.models-console-header`/`.models-cb-badge` 第二套合并到第一套
- ✅ 抽取共享拖拽排序: `createDragSortable(container, options)` 工具函数，替换 fallback 链拖拽(~80行) 和 provider 模型拖拽(~90行)，共减少 ~120 行
- ✅ renderConsole 增量更新: 已渲染时走增量路径（更新文本/badge/checkbox/Combobox值），避免 Combobox 销毁重建导致焦点丢失
- ✅ 瀑布流布局: grid `minmax(0,1fr) minmax(320px,0.92fr)` → `minmax(240px,0.6fr) minmax(320px,1fr)`，候选池更宽；`.connectors-filter` `flex:0 0 180px` → `flex:0 1 180px; min-width:120px`
- ✅ 瀑布流动画: JS 计算 scrollHeight 替代固定 max-height:2000px，展开/折叠更自然
- ✅ 控制台信息密度优化: 一键方案改为 `<details>` 折叠面板；推理开关移到 Combobox 旁边(.models-switch-left)；控制台 sticky 固定在顶部
- ✅ Provider 操作菜单: 点击切换 `.menu-open` class + 点击外部关闭，兼容 hover 和 touch
- ✅ 重构服务商布局为 Tab + 表格视图: 顶部 Tab 切换服务商(全部/deepseek/mimo/...)，下方统一表格展示模型列表（名称/服务商/状态/延迟/操作），替代 provider-card 折叠卡片布局
- ✅ 修复 `icon('plus')` 缺失: 在 icons.js PATHS 中添加 `'plus'` 定义
- 构建通过

### UI/UX Pro Max — 修复
- **`--text-3` 对比度**: 深色 `#52526E→#6868A0`，浅色 `#9ca3af→#a6a6b8`
- **表单状态**: 新增 error/success/disabled/hint/form-error class
- **`transition:all`**: `.btn` 拆分为显式属性
- **硬编码**: `nav-item gap:10px→--space-2`, `kernel-upgrade-hint` 对齐 token, `titlebar z-index:100000→--z-max`
- 构建通过

### 模型配置页面 — 桌面大屏两栏布局适配 (polish.css)
- **问题诊断**:
  1. `polish.css:422-424` 中 `.models-workbench .models-control-console` 设置了 `grid-template-columns`，但 `.models-control-console` 是 `display:flex; flex-direction:column`（models.css:280），grid 属性在 flex 容器上完全不生效，是死代码
  2. `.models-workbench` 是 flex column，大屏下所有内容竖排堆叠，宽度利用率低
  3. `@media (max-width:980px)` 中同样有无效的 `grid-template-columns: 1fr`
- **修复内容**:
  1. 删除无效的 `grid-template-columns`（flex 容器上不生效），添加注释说明
  2. 新增 `@media (min-width: 1200px)` 桌面两栏 grid 布局：
     - 左列 `minmax(380px, 440px)`：控制台（sticky）
     - 右列 `minmax(0, 1fr)`：服务商搜索 + 卡片/表格列表
     - 降级链展开时 `grid-column: 1 / -1` 跨全宽
  3. 右栏 `#providers-list` 网格列宽从 `560px` 收紧到 `420px`，大屏更多列

### 模型配置页面 — 最终布局方案（左侧窄栏控制台 + 右侧瀑布+列表）
- **核心思路**: 控制台占左侧 400px 窄栏（sticky），右栏纵向堆叠瀑布编辑器 + 服务商列表
- **布局网格**: `grid-template-columns: 400px 1fr; grid-template-rows: auto auto;`
  - 左栏(列1): console-container 跨两行 sticky，top: var(--space-4)
  - 右栏列2行1: fallback-waterfall-container（瀑布展开时全宽，生效链+候选池双栏）
  - 右栏列2行2: models-provider-workbench（搜索+Tab+卡片网格/表格）
- **瀑布编辑器双栏比例**: `minmax(260px, 0.45fr) minmax(320px, 1fr)`
- **服务列表网格列宽**: `minmax(460px, 100%)`
- **视觉统一 token 覆盖**: 控制台/瀑布面板/表格 → --shadow-1, --bg-elevated, --border-1；Tab active 态 → brand 色

### 模型配置页面 — 全组件设计系统对齐
- **背景**: 用户持续反馈 UI 不统一。审计发现 models.css ~120 个选择器使用旧 token（虽然不是视觉差异源），但大量组件完全没有被 polish.css 覆盖
- **一次性覆盖 30+ 组件**: 控制台/服务商 Tab 栏/卡片/模型行/表格/瀑布编辑器/效果链/预设按钮/Combobox/推理开关/批量栏/统计/标签/延迟徽章 — 全部使用 `--bg-elevated`/`--bg-sunken`/`--bg-base` + `--border-1` + `--shadow-1` + `--brand` 色系统
- **布局**: ≥1200px grid `400px 1fr`，控制台左栏 sticky 跨 row 1~20，右栏瀑布+列表纵向堆叠
