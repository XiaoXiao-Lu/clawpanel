# 2026-06-13 工作日志

## 技能管理清理与优化

### 移除 ~/.claude/skills/ 扫描
- **文件**: `src-tauri/src/commands/skills.rs`
- 删除 `custom_skill_roots_for_agent()` 中扫描 `~/.claude/skills/` 的代码（第569-574行）
- 原因：导致技能来源混乱、重复、版本冲突

### 简化技能去重逻辑
- **文件**: `src-tauri/src/commands/skills.rs`
- 删除 `dedupe_skill_entries()`、`should_replace_skill()`、`skill_rank()`、`preferred_cli_skill_path()` 四个函数（50+行）
- 改用 `HashSet` 按扫描顺序去重：用户目录先扫到即取，npm 内置同名自动跳过
- 同时在 `scan_custom_skill_detail()` 中去掉 `should_replace_skill` 调用，改为 roots 顺序优先

### 修复三个小问题
1. **skills.js Agent 筛选器提示** — 添加 `globalSkillHint` 显示"所有 Agent 共享全局技能库"
2. **saveSkills Gateway 状态检测** — 保存后调用 `getServicesStatus()` 检测 Gateway 状态，未运行则警告
3. **saveSkills 技能名合法性校验** — 保存前对照 skillsCatalog 检查勾选的技能名是否存在，不存在则阻止保存并警告

### 修复导航栏 Skills 页面问题
- **noSkillsHint 残留引用** — 删除所有 10 个 locale 文件和 1 个模块文件中 `~/.claude/skills/` 的引用，更新为仅提示 `~/.openclaw/skills/`
- **allAgents locale hack** — 替换 `"默认 (main)"` 为纯文本 `"默认"`，去掉 skills.js 中的 `.split('(')[0].trim()` 脏代码
- **Store 加载失败重试** — 失败页面添加重试按钮 `data-action="store-retry"`，点击重新加载商店
