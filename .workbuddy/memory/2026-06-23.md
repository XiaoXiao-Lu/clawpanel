# 2026-06-23 工作日志

- 修复渠道页面"内核不支持通用渠道运行态"误报问题：channels.status RPC 未在 FEATURE_CATALOG 注册 + 缺少 hasFeature 门控，导致 2026.6.9 内核连接后满屏黄条警告
  - feature-catalog.js: 新增 channels.runtimeStatus 条目 (minVersion: 2026.7.0 占位)
  - channels.js: loadChannelRuntimeStatus + 5 个 render 函数全部添加 hasFeature 门控
  - 构建通过 (2.97s)
- ⚠️ 回滚上述改动：minVersion 2026.7.0 远高于当前内核 2026.5.19，导致 hasFeature 始终返回 false，渠道页面的运行态徽章/摘要/操作按钮全部消失
  - feature-catalog.js: channels.runtimeStatus 条目改为注释（TODO），待上游内核正式支持后再启用
  - channels.js: `git checkout` 还原到原始版本，移除 hasFeature 导入和门控
  - 构建通过 (3.17s)
  - 根本原因：feature-catalog 中的条目会让 hasFeature 走严格模式检查 _snapshot.features.has()，而 buildSnapshot 根据 minVersion 过滤，当前内核版本不达标，导致该特性被判定为不可用
