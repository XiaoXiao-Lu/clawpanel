# 2026-06-06 工作日志

## Git 操作
- 提交并推送了 clawpanel 的修改（4 个文件），通过 PowerShell 通道解决 HTTPS 凭据问题

## Bug 修复

### 1. 飞书渠道保存时重复检查/安装插件
- **问题**: channels.js 中每次点击飞书渠道的"保存"都会调用 `getChannelPluginStatus`，如果插件检测不到就重新安装
- **修复**: 在 `openConfigDialog` 中添加 `_pluginChecked` 和 `_pluginInstalled` 缓存标志，同一次弹窗对话中只检查插件状态一次。首次保存检查/安装后，后续保存直接跳过。

### 2. 弹窗点击拖动内容容易关闭
- **问题**: models.js 和 modal.js 中的弹窗用 `click` 事件监听遮罩层关闭，用户拖动内容（滚动、选词等）时可能误触发关闭
- **修复**: 
  - modal.js 新增 `bindOverlayClose()` 辅助函数，使用 `pointerdown/pointerup` 追踪鼠标移动距离，移动超过 8px 视为拖动，不关闭弹窗
  - 应用到 `showConfirm`、`showModal`、`showContentModal`
  - models.js 的 `addProvider`、`addModel`、`bindModalEvents` 等 4 处弹窗也同步修复

### 3. 模型配置服务商缺少"获取模型列表"按钮
- **问题**: 服务商卡片上只有 `+ 模型` 和 `···` 按钮，没有"获取列表"按钮，用户只能手动填写模型 ID
- **修复**: 
  - 在服务商卡片头部加上了"获取列表"按钮，调用已有的 `fetchRemoteModels` 函数
  - 在 i18n 文件添加了 `fetchModels` 翻译键

## 运行时问题排查

### 1. realistic-photo-prompts 技能路径错误
- **问题**: Gateway AI 尝试读取 `~/skills/realistic-photo-prompts/SKILL.md`（404），但技能实际存放在 `~/.workbuddy/skills/`
- **修复**: 创建了目录符号链接 `~/skills` → `~/.workbuddy/skills`

### 2. 三个技能缺少名称 frontmatter
- **修复**: 
  - `kie-image-gen` — 添加了 name/description frontmatter，更新了 docs 链接为 `/cn`
  - `agent-memory` — 添加了 name/description frontmatter
  - `codeconductor` — 创建了 SKILL.md 并添加了 frontmatter（原文件名为 skills.md 而非 SKILL.md）

### 3. docs.kie.ai 文档链接失效
- **问题**: 文档站点结构改版，`/api-reference` 路径返回 404
- **修复**: 更新了 kie-image-gen 技能中的文档链接为 `https://docs.kie.ai/cn`

### 4. Web Search 和 Image 工具未配置
- **说明**: 这是 Gateway AI 的运行时问题，需要配置对应的搜索服务商和图片生成服务商

### 5. 技能列表显示名称和来源问题 (skills.rs)
- **问题1**: 技能 name 字段使用目录名（如 skill_2053082127698788352），未从 SKILL.md frontmatter 读取
- **问题2**: 自定义技能（无 package.json）与 marketplace 技能无法区分
- **修复**:
  - `scan_single_skill`: 优先从 SKILL.md frontmatter 读取 name 字段，目录名仅作 fallback
  - `scan_local_skill_entries_for_agent`: 使用 frontmatter 中的 name 显示
  - 无 package.json 的技能自动标记为 `source: "自定义"`，有 package.json 的按目录来源标记

### 6. 清理重复技能
- **删除 3 组重复技能**（.openclaw/skills 和 .workbuddy/skills 两边同步清理）：
  - `find-skill/` → 保留 `find-skills/`
  - `api-gateway-1-0-31/` → 保留 `api-gateway/`
  - `openclaw-tavily-search/` → 保留 `tavilysearch/`

## 全面代码审查（Bug/性能/交互/UI）

详见对话中的完整报告。主要发现：

### Bug（4个）
1. `router.js` `withTimeout` 内存泄漏 — Promise.race 不清除 setTimeout
2. `main.js` 401 拦截函数直接 reload() 丢失用户状态
3. `index.html` 第 1 行 BOM 字符可能引发渲染问题
4. `kernel.js` `parseVersion` 对空参数 `ver=0` 误判

### 性能（5个）
1. `sidebar.js` 每次全量重建 innerHTML — 应改用 DOM diff
2. `chat.js` 无消息虚拟列表 — 长会话 DOM 膨胀
3. `tauri-api.js` 缓存 TTL 仅 15s — 高频页面重复请求
4. `settings.js` 29 次 innerHTML 赋值 — 应批量构建
5. `ws-client.js` 消息事件缓存无上限清理

### 交互（5个）
1. nav-item 36px 触控目标 < 44px WCAG 标准
2. chat 消息粘贴大文本无确认保护
3. 弹窗无 ESC 键关闭支持
4. Gateway 横幅消失时机不明确
5. 部分页面 cleanup() 为空，资源泄露

### UI/CSS（5个）
1. `color-mix()` 浏览器兼容性差（Chrome 111+）
2. `backdrop-filter` 无降级方案
3. 全局 `prefers-reduced-motion` 缺失
4. `font-family` 使用 `ui-sans-serif`（Safari 专有）
5. 暗色主题对比度不足（text-tertiary 仅 3.2:1）

## 19项修复全部完成（Bug/性能/交互/UI）

通过 BugFix 快捷路径（software-clawpanel-fix 团队），工程师+QA 协作完成：
- 修改文件：`router.js`, `main.js`, `index.html`, `kernel.js`, `sidebar.js`, `chat.js`, `tauri-api.js`, `settings.js`, `ws-client.js`, `modal.js`, `channels.js`, `chat-debug.js`, `layout.css`, `chat.css`, `variables.css`，以及 11 个 locale 文件
- QA 最终 IS_PASS: YES，19/19 检查项通过

## 飞书/QQ渠道连接失败根因诊断

通过实际运行环境诊断，确认根因：

### 飞书 (@larksuite/openclaw-lark)
- 状态：`openclaw status --deep` 显示 **OFF - not configured**
- 凭据有效（API 实测返回 tenant_access_token）
- 插件已安装（npm/projects/larksuite-openclaw-lark-* 存在）
- **根因**：插件 `accounts.js:122-123` 对 default 账户**不读取** `accounts.default`，只读取 `channels.feishu.{appId, appSecret}`（顶层）
- ClawPanel 的 `messaging.rs` 把凭据写入了 `channels.feishu.accounts.default.{appId, appSecret}`
- → 凭证位置与插件读取路径不匹配，导致 `configured: false`

### QQ Bot (qqbot)
- 状态：`openclaw status --deep` 显示 **OFF - not configured**
- 凭据有效（API 实测返回 access_token）
- 插件已安装（npm/projects/tencent-connect-openclaw-qqbot-* 存在）
- **根因**：同样的问题 — 凭据在 `channels.qqbot.accounts.default` 而非 `channels.qqbot` 顶层
- 额外问题：`plugins.allow` 需要同时有 `"qqbot"`（当前已有）

### Telegram（正常工作，用于对比）
- 凭据在 `channels.telegram.botToken`（顶层），插件能正确读取

## openclaw 渠道配置重构（修复 default 账户凭据保存位置）

### 根因
- `merge_channel_entry_for_account` 对 account_id="default" 写入 `channels.<channel>.accounts.default`，但飞书/QQ插件只读 `channels.<channel>` 顶层
- 对照 Telegram：凭据在 `channels.telegram.botToken` 顶层，正常工作

### 修改（software-bugfix-channel-config 团队，engineer 执行）
1. `merge_channel_entry_for_account` (messaging.rs:1260-1272)：account_id="default" 时调用 `merge_channel_entry` 写入渠道顶层
2. qqbot save (messaging.rs:2491-2521)：default 账户凭据写入 channels.qqbot，非 default 写入 accounts
3. 迁移现有 openclaw.json：feishu/qqbot 凭据从 accounts.default 提升到 channels 顶层
4. 备份：C:\Users\Kinnon\.openclaw\openclaw.json.bak
5. cargo check 通过

### 验证结果（software-bugfix-channel-verify 团队，qa-verify 执行）
- openclaw.json 凭据已成功迁移到顶层，accounts.default 已移除
- `openclaw status --deep`: QQ Bot → OK | configured ✅，Feishu → OK | configured ✅
- IS_PASS: YES，无需重启 Gateway 即时生效

